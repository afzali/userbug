# نقشهٔ اپ — از «صفحه‌هایی که کاربر نشانمان داد» تا «هر جایی که می‌شود رفت»

این فایل **طرح** است، نه گزارش. هر بندش یک تصمیم دارد و دلیلش. وضعیتِ کنارِ هر
قدم، حالِ امروز است.

مرجعِ بالادست: [ONBOARDING.md](ONBOARDING.md) — پروندهٔ شناخت، گشت، و چک‌ها.
این فایل قطعهٔ گم‌شدهٔ همان طرح است.

---

## مسئله در یک جمله

گشت آن‌جایی را می‌شناسد که **کاربر برد**. هضمِ سورس روت‌ها را می‌داند ولی
نمی‌داند به هرکدام **چطور** می‌رسند. و `explore` هر بار از نو می‌گردد، یک
فراخوانی مدل به ازای هر قدم، و در پایان یک تاریخچه می‌دهد نه یک نقشه.

پس سه چیز نداریم:

- **فهرستِ کامل** — «چند شاخه داریم؟» جواب ندارد جز حدس.
- **مسیرِ رسیدن** — پیشنهادِ `propose.js` می‌گوید «`/settings` را بیازما» و
  سناریوی تولیدشده `go: /settings` می‌زند؛ در یک SPAی لاگین‌دار می‌افتد.
- **تکرارپذیری** — همان کشفی که دیروز شد، امروز دوباره با پول انجام می‌شود.

## راه‌حل در یک جمله

یک **گرافِ حالت** برای هر پروژه، که یک بار گران کشف می‌شود و بعد بی‌مدل
تکرارپذیر است: گره = حالتی که کاربر می‌بیند، یال = کنشی که او را از این حالت
به آن حالت برد.

و **واحدِ نقشه «صفحه» نیست، «حالت» است.**

---

# بخش ۱ — واحدِ نقشه

## چرا آدرس واحدِ درستی نیست

دو شکستِ متقارن:

- فهرستِ ۲۰۰ یادداشتیِ نپی، ۲۰۰ آدرس است با یک ساختار. اگر آدرس واحد باشد،
  نقشه ۲۰۰ گره می‌گیرد با ۱۲ دکمهٔ یکسان = ۲۴۰۰ کلیک برای یک دانش.
- مودال و کشو **هیچ** آدرسی ندارند. اگر آدرس واحد باشد، نیمی از یک اپِ امروزی
  در نقشه دیده نمی‌شود — و همان نیمه است که هیچ‌کس تست ننوشته.

## شکلِ هویتِ حالت

```
stateId = hash( الگوی روت | نما | نمای نقش‌ها )
```

- **الگوی روت** — `/content/6737f33d35` و `/content/abc123` هر دو
  `/content/[id]`. اگر پرونده روتِ سورس‌داری دارد که به آدرس بخورد، همان
  برنده است (`[id]` از کد دقیق‌تر از حدسِ ما است)؛ وگرنه قاعده: قطعه‌ای که
  شبیه شناسه است (هگزِ ۶+، عدد، uuid) `[id]` می‌شود.
- **نما** — همان `detectView()ِ` گشت: مودال/کشوی باز. `/contents` با مودالِ
  «وارد کردن اطلاعات» گرهِ دیگری است.
- **نمای نقش‌ها** — فهرستِ مرتبِ `role:count` از snapshot. **بی هیچ متنی.**

**چرا متن در هویت نیست.** نامِ عناصر روی صفحهٔ محتوا خودِ محتواست (در گشتِ
واقعی یک `role: presentation` با ۸۰ نویسه متنِ کتاب ثبت شده). هر متنی که
وارد هویت شود، هر یادداشت را یک گرهِ تازه می‌کند و نقشه می‌شود همان فهرستِ
داده‌ای که نمی‌خواستیم.

**و چرا شمارشِ نقش‌ها کافی است.** به‌تنهایی نیست — دو صفحهٔ واقعاً متفاوت
می‌توانند یک نمای نقش داشته باشند. الگوی روت و نما همان‌جا کنارش هستند و
تفکیک را انجام می‌دهند. هویتی که خیلی حساس باشد نقشه را منفجر می‌کند و
هویتی که خیلی کر باشد دو صفحه را یکی می‌خواند؛ از این دو، دومی قابلِ
دیدن است (در فهرستِ کنش‌های همان گره) و اولی نیست.

## کنش‌ها: اجتماع، با شمارشِ بازدید

هر بازدیدِ یک گره ممکن است فهرستِ کنشِ کمی متفاوت بدهد (عنوانِ یادداشت‌ها فرق
می‌کند). پس فهرست **اجتماع** می‌شود و هر کنش `seenIn` دارد — دقیقاً همان
«تقویت با تقاطع» که `checks/contract.js` برای قرارداد انجام می‌دهد. کنشی که
در همهٔ بازدیدها بوده، پوستهٔ اپ است؛ کنشی که یک بار دیده شده، داده است.

## سه نمونه از هر خانواده، نه همه

صفحه‌ای با ۱۰۰ پیوندِ یادداشت، ۱۰۰ کنش دارد که همه یک چیز را اثبات می‌کنند.
پس در هر گره، از هر `role` حداکثر **سه** نمونه امتحان می‌شود و بقیه
`sampled: false` ثبت می‌شوند — هستند، شمرده می‌شوند، ولی کلیک نمی‌گیرند.

**چرا سه و نه یک:** اولی ممکن است ردیفِ خاصی باشد (اولین، یا تنها ردیفِ
بی‌داده). سه نمونه، «این خانواده کار می‌کند» را می‌گوید بی‌آنکه بودجه را
بخورد.

---

# بخش ۲ — گراف، نه درخت

```jsonc
{
  "version": 1, "target": "nepi2", "baseURL": "http://localhost:5173",
  "caps": { "states": 60, "actionsPerState": 25, "minutes": 20 },
  "entry": { "scenario": "scenarios/nepi2/ورود.yml", "steps": 8 },

  "states": [{
    "id": "a1b2c3d4",
    "route": "/content/[id]", "sample": "/content/6737f33d35", "view": "",
    "title": "نماز",
    "profile": "button:12,link:4,textbox:2",
    "path": [ { "click": { "role": "link", "name": "نماز" } } ],
    "visits": 3, "firstSeen": "…", "lastSeen": "…",
    "actions": [{
      "key": "…", "descriptor": { "role": "button", "name": "فهرست" },
      "kind": "unknown", "by": "rule", "seenIn": 3,
      "tried": true, "to": "c3d4e5f6", "sampled": true
    }]
  }],

  "edges": [{ "from": "a1b2c3d4", "action": "…", "to": "c3d4e5f6", "at": "…" }],
  "frontier": [{ "state": "a1b2c3d4", "action": "…" }],
  "stats": { "states": 12, "edges": 34, "tried": 51, "skipped": 7, "findings": 3 }
}
```

**`path` تمامِ ارزشِ یال است.** کوتاه‌ترین زنجیرهٔ قدم‌های رسیدن به هر گره،
به‌شکلِ همان فعل‌های سناریو. یعنی هر گره از روزِ اول یک مقدمهٔ اجراپذیر دارد،
و سناریویی که بعداً برایش ساخته شود واقعاً اجرا می‌شود. بدون این، نقشه یک
فهرستِ قشنگ است که هیچ سناریویی از آن درنمی‌آید.

**چرا `frontier` روی دیسک است.** خزشِ چهل‌دقیقه‌ای که در دقیقهٔ ۳۹ بشکند باید
از همان‌جا ادامه بدهد، نه از صفر. همان درسی که `explore.js` بعد از سوختنِ هشت
قدم گرفت: چیزی که فقط در پایان نوشته شود، دقیقاً وقتی لازم است که نیست.

---

# بخش ۳ — چه چیزی مدل می‌خواهد و چه چیزی نه

| کار | ابزار | هزینه |
|---|---|---|
| کشفِ گره و یال | DOM + پیمایش | صفر |
| الگوی روت | قاعده + روت‌های سورس | صفر |
| «این دکمه ناوبری است یا جهش؟» | سورسِ کامپوننت، با تطبیقِ متنِ برچسب | صفر برای بیشترش |
| «این دکمه خطرناک است؟» | قاعده + یک فراخوانی به ازای هر گره، کش‌شده | یک بار |
| نامِ خوانا و `purpose` و دسته‌بندی | یک فراخوانی روی کلِ نقشه | یک بار |
| اجرای سناریوهای ساخته‌شده | کشِ `_learned` | صفر، همیشه |

**قاعده‌اش همان `routes.js` است:** فهرستِ کنش‌های یک صفحه یک فکتِ ساختاری
است. سپردنش به مدل یعنی پول دادن برای چیزی که `querySelectorAll` جواب می‌دهد.

## باطل‌سازیِ دوتایی

طبقه‌بندیِ کنش‌ها (آن یک فراخوانی به ازای هر گره) با دو مهر باطل می‌شود:

- **`profile` گره** — سمتِ رندر. عوض شد یعنی صفحه عوض شده.
- **هشِ فایل‌های سورسی که به این گره نسبت داده شده‌اند** — سمتِ کد.

هر کدام نخورد، **فقط همان گره** دوباره طبقه‌بندی می‌شود.

**چرا هشِ کلِ ریپو نه.** هر کامیت، کلِ نقشه را باطل می‌کرد. و چرا فقط گیت
کافی نیست: صفحه می‌تواند بی‌تغییرِ سورس عوض شود (داده، فلگ، تغییرِ بک) و
کامیت می‌تواند به این صفحه هیچ کاری نداشته باشد.

**نسبت دادنِ نودِ DOM به خطِ سورس با سلکتور کار نمی‌کند** — کلاس‌های تیلویند و
شناسه‌های `bits-cNN` که `signature.js` درباره‌شان نوشته. تطبیق روی **متنِ
برچسب** است، همان کاری که `from-text.js` با نرمال‌سازیِ نیم‌فاصله می‌کند.

---

# بخش ۴ — ایمنی: کلیک، برخلاف ناوبری، حالت را عوض می‌کند

چهار دسته، به ترتیبِ محافظت:

| دسته | از کجا معلوم می‌شود | با آن چه می‌کنیم |
|---|---|---|
| `avoided` | `explore.avoid` + `risks` پرونده | به مدل هم نشان داده نمی‌شود؛ کلیک نمی‌شود |
| `destructive` | قاعدهٔ واژگانی (حذف/پاک/ریست/خروج) و بعد سورس | ثبت می‌شود، کلیک نمی‌شود مگر با پرچمِ صریح |
| `nav` | `a[href]` یا `goto(...)` در سورس | امن؛ و اگر آدرس دارد، بی‌کلیک هم می‌شود رفت |
| `unknown` | بقیه | کلیک می‌شود، زیرِ سقف‌ها |

**خروج، خودزنیِ نقشه است.** عاملی که آزاد بگردد دیر یا زود «خروج» را می‌زند و
بقیهٔ خزش روی صفحهٔ ورود می‌گذرد — همان درسی که `explore.js` نوشته. با این
تفاوت که آنجا یک کاوشِ دوازده‌قدمی هدر می‌رفت و اینجا یک خزشِ چهل‌دقیقه‌ای.

**روی `production` هیچ کلیکی نمی‌شود.** نقشهٔ محیطِ تولیدی فقط از راهِ
ناوبری ساخته می‌شود. دروازه‌اش همان `guard.js` است، با همان استدلال.

---

# بخش ۵ — خزش، سه خروجی دارد و سومی گران‌ترین است

1. **نقشه** — `knowledge/<کلید>/map.json`.
2. **کش** — هر یالِ تأییدشده یک مدخلِ `_learned` با `resolvedBy: 'crawl'`، پس
   سناریویی که از نقشه ساخته شود **از اجرای اول** بی‌مدل می‌رود.
3. **یافته** — و این را دست‌کم نگیرید: همه‌چیز از `ub.step` رد می‌شود، پس
   `checks/universal.js` روی **هر** کلیک اجرا می‌شود. خزشی که همهٔ دکمه‌های
   اپ را بزند، پرکاوش‌ترین شکارِ باگی است که این ابزار داشته. نقشه بهانه است؛
   این محصولِ جانبیِ گران‌تر است.

## و یک بردِ مجانی: پیش‌بینی در برابر واقعیت

سورس می‌گوید این دکمه به `/a` می‌رود؛ کلیک به `/login` می‌رسد. این اختلاف صفر
هزینه دارد و جنسِ باگی است که هیچ چکِ همگانی نمی‌گیرد.

## و تفاضلِ سورس و خزش

روتی که در کد هست و در مرورگر به آن نرسیدیم (یتیم، یا نیازمندِ حالتی که
نساختیم) و روتی که دیدیم و در کد نیست. صفر فراخوانی.

---

# بخش ۶ — ورود: نقشه از جایی شروع می‌شود که گشت تمام کرد

اپی که ثبت‌نام و ورود دارد، برای ناشناس یک صفحه است. پس خزش یک **مسیرِ ورود**
می‌گیرد: `--from <سناریو>`، که قدم‌هایش پیش از شروع بازپخش می‌شوند و مسیرِ هر
گره از همان‌جا حساب می‌شود.

**چرا سناریو و نه «حسابِ ذخیره‌شده».** «چطور وارد می‌شوند» خودش دانش است و جای
نوشتنش سناریوست، نه پرچمِ CLI. و آن سناریو از قبل وجود دارد: خروجیِ گشت.
همان‌جا که کاربر یک بار لاگین کرد.

**پیشنهاد: یک سناریوی کوچکِ «ورود»، نه پیش‌نویسِ کاملِ گشت.** پیش‌نویسِ گشت
(۸۰ قدم، با `upload` و مسیرِ اشتباه و کلیکِ تکراری) مقدمهٔ شکننده‌ای است. خزش
هر فعلِ ناشناسی را **بلند** رد می‌کند، نه اینکه بی‌صدا از آن بگذرد.

---

# آنچه نخستین خزش‌های واقعی عوض کرد

فاز ۱ روی نپی نوشته و اجرا شد. هفت چیز که **فقط** با اجرای واقعی معلوم شدند و
هرکدام یک تصمیمِ طرح را عوض کردند:

**۱. مکثِ ثابت، صفحه را در حالِ بارگذاری می‌خواند.** نخستین نقشه یک گره داشت با
یک کنش: «در حال بارگذاری نپی…». جایش سنجشِ نشستن آمد — دو نمونهٔ پیاپیِ یکسان
از همان **نمای نقش‌ها** که هویتِ گره است.

**۲. ولی اسپینر هم پایدار است.** معیارِ پایداری خودش راضی می‌شد. پس شرطِ دوم:
صفحه باید دستِ‌کم یک کنشِ **غیرخبری** داشته باشد. `status`، `progressbar` و
هم‌خانواده‌هایشان کنش نیستند (و هرکدام ۵ ثانیه timeout می‌سوزاندند).

**۳. مزاحم باید پس از نشستن بسته شود، و چند دور.** بستن درست بعد از `go` هیچ
پنجره‌ای نمی‌دید. و بستنِ اولی، دومی را رو می‌کند: «بروزرسانی دیتابیس» و پشتش
«محافظت از داده‌ها». منطقش از `fixtures.js` به `src/observe/blockers.js`
بیرون کشیده شد تا دو تعریف نداشته باشیم.

**۴. مسیرِ ورود باید از هر دو حالت کار کند.** خزش دهها بار به خانه برمی‌گردد و
بارِ دوم کاربر از قبل وارد است. با `fill`ِ بی‌شرط، همان‌جا می‌شکست. راهش
`when/then` بود — فعلی که از قبل در سناریوها هست، نه کلیدِ اختراعی.

**۵. «سه نمونه از هر نقش» نقشه را پوچ کرد.** `/contents` نپی ۱۸ دکمهٔ متفاوت
دارد و فقط ۳ تا خزیده شد. `role` وقتی نشانهٔ خانواده است که **جمعیت** داشته
باشد؛ آستانه به ۲۰ رفت و سقفِ گره کارِ اصلیِ محدودسازی را می‌کند.

**۶. شمارشِ دقیق، هویت را شکننده کرد.** همان صفحه یک بار ۱۸ دکمه داشت و بار
بعد ۲۰ (یک toast، یک ردیفِ تازه). هر نوسان یک «گرهِ تازه» می‌ساخت و مسیرها
پشت سر هم می‌شکستند. شمارش سطل‌بندی شد (`17-32`)، پس تفاوتِ مرتبه‌ای هنوز
گره را جدا می‌کند و نوسان نه.

**۷. برگشت به گره، گران‌ترین کارِ خزش است.** حلقهٔ سطح‌اولِ خالص برای هر کنش
مسیرِ ورود را بازپخش می‌کرد: ۱۵ تا ۳۰ ثانیه، و ده دقیقه صرفِ ۱۸ کنش شد. حالا
اگر گرهی که در آن ایستاده‌ایم کنشِ نیازموده دارد، همان زده می‌شود و صف فقط
وقتی لازم می‌شود که جایی گیر کنیم.

و یک چیزِ هشتم که **طرح را تأیید کرد**: گره‌های بی‌نامِ `/contents` واقعاً
حالت‌های متفاوتی بودند — منوی باز، listboxِ باز، breadcrumb. تشخیصِ نما فقط
مودال را می‌دید؛ حالا منو و فهرست هم نام می‌گیرند.

## گران‌ترین چیز، اندازه‌گیری‌شده

نخستین سنجش گفت هر کنش ۶.۵ ثانیه می‌گیرد — ولی میانهٔ قدم‌ها ۵.۴ ثانیه بود و
خوشهٔ عجیبی داشت: **بیش از نیمی از کلیک‌ها اصلاً موفق نمی‌شدند** و هرکدام
تمامِ مهلتِ پنج‌ثانیه‌ای را می‌سوزاندند. یعنی مسئله سرعت نبود، هدر بود.

| اصلاح | حالت | یال | کنش | شکست | هر قدم |
|---|---|---|---|---|---|
| نقطهٔ شروع | ۱۴ | ۱۵ | ۳۸ | ۲۰ (۵۳٪) | ۶.۵s |
| `blocked` در snapshot | ۱۵ | ۲۰ | ۴۵ | ۱۷ (۳۸٪) | ۵.۳s |
| نقشِ ظرف ≠ کنش | ۱۵ | ۲۵ | ۵۴ | ۱۷ (۳۱٪) | ۴.۵s |
| شکستِ تکراری دوباره امتحان نشود | ۱۷ | ۲۹ | ۵۸ | ۱۵ (۲۶٪) | ۴.۱s |

(همه با بودجهٔ یکسانِ چهار دقیقه روی نپی.)

سه چیزی که این سه اصلاح را ساخت:

- **`elementFromPoint`، نه فهرستِ لایه‌ها.** نسخهٔ اول `[role=dialog]` را
  لایه می‌دانست و هرچه بیرونش بود را مسدود؛ اجرای بعدی نشان داد منو و کشو هم
  می‌گیرند. فهرستِ نقش‌های «لایه» هیچ‌وقت کامل نمی‌شود. حالا همان چیزی پرسیده
  می‌شود که مرورگر پیش از کلیک می‌سنجد.
- **قابِ مودال کنش نیست.** خودِ `[role="dialog"]` نام دارد و در snapshot
  می‌آید، پس هر مودال دقیقاً یک شکستِ پنج‌ثانیه‌ای می‌داد.
- **شکست، فکت است نه اتفاق.** یک دکمهٔ نوارِ کناری در دوازده حالت امتحان شد و
  دوازده بار افتاد. حالا بعد از دو شکست، همان توصیف دیگر امتحان نمی‌شود.

**۲۶٪ شکستِ باقی‌مانده عمدتاً مسابقه است، نه باگ:** عنصری که بین گرفتنِ
snapshot و رسیدنِ کلیک ناپدید می‌شود. تبدیلش به یافته وسوسه‌انگیز است و
اشتباه — همان «یافتهٔ بی‌بازتولید» می‌شود.

---

# فازها

| # | فاز | خروجی | مدل | وضعیت |
|---|---|---|---|---|
| ۱ | **کشفِ قطعی** — گره، یال، مسیر، سقف، ادامه‌پذیری | `map.json` + یافته‌ها | ❌ | ✅ |
| ۳ | **دسته‌بندی و نما** — خانوادهٔ روت → حالت‌ها → کنش‌ها، در رابط | صفحهٔ نقشه | ❌ | ✅ بی‌مدل درآمد |
| ۴ | **نقشه → پیشنهادِ سناریو** — با `path` به‌عنوان مقدمه | سناریوی اجراپذیر | همان مسیرِ موجود | ✅ |
| ۲ | **طبقه‌بندیِ کنش‌ها** از سورس + یک فراخوانی به ازای گره، با باطل‌سازیِ دوتایی | `kind` و `to`ِ پیش‌بینی‌شده | یک بار | ⬜ |
| ۵ | **خزشِ دوباره = کهنگی‌سنج** — گرهِ تازه، گرهِ ناپدید | همان «سناریوی داینامیک» | ❌ | ⬜ |

ترتیب عوض شد و دلیلش ارزش نوشتن دارد: **فاز ۳ به مدل نیاز نداشت.** قرار بود
دسته‌بندی را مدل بنویسد، ولی دو محورِ رایگان از قبل در داده بود — خانوادهٔ روت
و نوعِ لایه — و همان‌ها صفحه را خوانا کردند. پس فاز ۲ (تنها فازِ مدل‌دار) به
آخر رفت، چون معلوم شد چیزی که بدونش کار نمی‌کند نیست.

## فاز ۴، همان‌طور که ساخته شد

هر گرهی که **نما** دارد و هیچ سناویی نامش را نبرده، یک پیشنهاد می‌شود. سه
تصمیم:

- **فقط مودال، نه منو و کشو.** نامِ منو از متنِ دکمه‌اش می‌آید و در نپی یکی‌شان
  ایمیلِ کاربرِ همان اجرا بود. پیشنهادِ «منوی ub-620883b2@userbug.test» هم
  بی‌معناست هم ناپایدار، و فهرستی که دو بار چیزِ بی‌ربط بدهد بار سوم بسته
  می‌شود.
- **مقدمه از کلاینت نمی‌آید.** دکمهٔ «بساز» فقط **شناسهٔ پیشنهاد** را می‌فرستد
  و سرور خودش مسیر را از نقشه برمی‌دارد. قدمِ سناریو کنشِ مرورگر است؛ گرفتنش
  از مرورگر یعنی یک سطحِ حمله برای چیزی که از قبل روی دیسک هست.
- **مرزِ قطعی و حدس در خودِ فایل نوشته می‌شود.** سرصفحهٔ YAML می‌گوید n قدمِ
  اول از نقشه آمده و بقیه از مدل. بی این، بازبین هر دو را یک‌جور می‌خواند و
  وقتی سناریو بشکند نمی‌داند از کدام نیمه شروع کند.

---

# پنج اصطکاکی که پنهان نمی‌شوند

**۱. «همهٔ حالات» دست‌نیافتنی است.** حالتِ وابسته به داده بی‌نهایت است. نقشه
یعنی «هرچه زیرِ سقف‌های اعلام‌شده قابل دسترسی بود»، و عددِ پوشش باید همین را
بگوید. ادعای «کامل» بدترین چیزی است که این فایل می‌تواند تولید کند.

**۲. بازپخشِ مسیر، هزینهٔ زمانی دارد.** برای امتحانِ کنشِ پنجمِ یک گره باید
مسیرش دوباره طی شود. تخفیفش این است: کنشی که حالت را عوض نکرد بازپخش نمی‌خواهد،
و کنش‌های یک گره پشت سر هم امتحان می‌شوند. ولی خزشِ یک اپِ متوسط دقیقه‌ها طول
می‌کشد و این ذاتی است، نه نقص.

**۳. مسیرِ ذخیره‌شده می‌شکند.** اپ عوض می‌شود و مسیرِ دیروز به گرهِ دیگری
می‌رسد. پس پیش از هر کنش، `profile`ِ حالتِ فعلی سنجیده می‌شود؛ نخورد، آن آیتم
`pathBroken` می‌گیرد و گره برای بازکشف علامت می‌خورد. بازپخشی که بی‌بررسی
ادامه بدهد، یال‌های دروغ می‌سازد.

**۴. خزش داده تولید می‌کند.** صد کلیک روی یک اپِ محلی صد یادداشتِ آشغال
می‌سازد. `isolation.reset` موجود همین را حل می‌کند، ولی باید صریح باشد که
خزش **جهش‌زاست** و روی محیطی که داده‌اش مهم است نباید زد.

**۵. نقشه به مدل می‌رود (در فاز ۲).** همان مرزِ اعتمادی که `source-access.js`
با چهار قاعده حفظش کرد: `redactDeep` پیش از هر ارسال.

---

# آنچه این طرح **نمی‌کند**

- **جای گشت را نمی‌گیرد.** گشت `purpose` و جملهٔ آدم را می‌آورد که هیچ خزشی
  نمی‌آورد. نقشه می‌گوید «کجاها می‌شود رفت»، گشت می‌گوید «اینجا برای چیست».
- **جای `explore` را نمی‌گیرد.** آن با هدف می‌گردد و می‌تواند فرم پر کند؛ این
  فهرست می‌سازد.
- **فرم پر نمی‌کند** (فاز ۱). ورودی‌ها ثبت می‌شوند و کلیک نمی‌گیرند؛ پر کردنِ
  کورکورانه یعنی داده‌ای که بعداً هیچ‌کس نمی‌داند از کجا آمد.
- **`expect` نمی‌سازد.** نقشه رفتارِ **فعلی** را ضبط می‌کند. هر سنجشی از آن
  درآید `watch` است تا آدم تأییدش کند — همان تفاوتِ `watch`/`expect` در
  ONBOARDING بخش ۸.
