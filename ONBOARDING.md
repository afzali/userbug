# آشنایی با پروژه — از «هدفِ ثبت‌شده» تا «سامانه‌ای که می‌شناسیم»

این فایل **طرح** است، نه گزارش. هر بندش یک تصمیم دارد و دلیلش. وضعیتِ کنارِ هر
قدم، حالِ امروز است.

---

## مسئله در یک جمله

امروز userbug **اپ را می‌بیند ولی نمی‌شناسد.** هر بار که مدل چیزی می‌نویسد،
از صفر شروع می‌کند: `from-text.js` با چند واژه در سورس جست‌وجو می‌کند،
`explore.js` بی‌نقشه کلیک می‌کند، `do.js` هر قدم را جدا حل می‌کند. هیچ‌کدام
نمی‌دانند این اپ چیست، چه روت‌هایی دارد، ورودش چطور است، و کدام صفحه برای چه
کاری ساخته شده.

نتیجه‌اش سه چیز است که در همین مخزن ثبت شده:

- کاوشگر ده قدم از دوازده قدم را روی فرضِ غلط سوزاند («منویی با گزینهٔ سند»
  که وجود نداشت) — `TODO.md`، بدهی فنی.
- سناریوی ساخته‌شده از متن، در `notes` صادقانه می‌نویسد «بر اساس الگوهای رایج
  فرض شده» — یعنی برچسب دکمه را حدس زده.
- `explore.avoid` دستی در کانفیگ نپی نوشته شده، چون هیچ‌کس به ابزار نگفته بود
  «ریست کامل» خطرناک است.

هر سه یک ریشه دارند: **جایی برای نگه‌داشتنِ شناخت نیست.**

## راه‌حل در یک جمله

یک **پروندهٔ شناخت** برای هر پروژه، که از چهار راه پر می‌شود — گشتِ زندهٔ
کاربر در مرورگر، هضمِ سورس، پرسش از کاربر، و مستندات — و هر بار که مدل کاری
می‌کند، همان پرونده جلوی چشمش است.

و **نخستین چیزی که یک پروژهٔ تازه می‌گیرد یک سناریو نیست، یک گشت است.**

---

# بخش ۱ — پروندهٔ شناخت

## کجا می‌نشیند و چرا آنجا

```
knowledge/<پروژه>/
  dossier.json          ← ساختاریافته. تنها منبعِ حقیقت.
  pages/<slug>.json     ← یک فایل به ازای هر صفحهٔ شناخته‌شده
  tours/<id>/           ← خامِ هر گشت: رخدادها، عکس‌ها، قدم‌های ضبط‌شده
  fixtures/             ← فایل‌هایی که سناریو آپلود می‌کند
  credentials.json      ← اختیاری، در .gitignore
```

کنارِ `targets/<پروژه>.config.js` و `scenarios/<پروژه>/` می‌نشیند و همان الگو
را دنبال می‌کند: هر پروژه یک پوشه، نامش کلیدِ پروژه.

**چرا داخل کانفیگ هدف نمی‌رود.** کانفیگ فایل جاوااسکریپتی است که کاربر
می‌نویسد و ابزار فقط می‌خواندش. پرونده برعکس است: ابزار می‌نویسدش و هر
جلسه بزرگ‌تر می‌شود. یکی کردنشان یعنی ابزار فایلی را بازنویسی کند که
کامنت‌های دست‌نویسِ کاربر در آن است — و کامنت‌های کانفیگ نپی نشان می‌دهند آن
کامنت‌ها گاهی از خودِ کد ارزشمندترند.

**چرا JSON و نه Markdown.** پرونده را مدل می‌خواند و رابط ویرایش می‌کند. یک
`dossier.md` **ساخته می‌شود** برای خواندنِ آدم، ولی مبدأش JSON است. دو منبع
یعنی واگرایی، و اینجا واگرایی یعنی مدل چیزی می‌بیند که کاربر ندیده.

## شکل `dossier.json`

```jsonc
{
  "version": 1,
  "target": "nepi",
  "updatedAt": "…",

  "summary": "یک پاراگراف: این اپ چیست و کاربرش چه می‌خواهد",
  "stack": { "framework": "sveltekit", "language": "js", "backend": "php", "db": "sqlite" },

  "auth": {
    "kind": "form",              // form | oauth | magic-link | none
    "loginPath": "/login",
    "signupOpen": true,
    "logoutLabel": "خروج",
    "sessionStore": "localStorage"
  },

  "routes": [
    { "path": "/library", "title": "کتابخانه", "purpose": "…", "requiresAuth": true,
      "sourceFile": "src/routes/library/+page.svelte", "by": "source" }
  ],

  "glossary":  [{ "term": "سند", "meaning": "…", "by": "user" }],
  "entities":  [{ "name": "document", "label": "سند", "where": "…" }],
  "flows":     [{ "name": "ورود", "scenario": "learned-login.yml", "steps": ["…"] }],
  "files":     { "uploads": [], "downloads": [{ "where": "/settings", "what": "کد بازیابی", "format": "txt" }] },
  "risks":     [{ "label": "ریست کامل", "why": "دیتابیس را پاک می‌کند" }],
  "openQuestions": [{ "q": "…", "askedAt": "…", "answer": null }],

  "sources": [{ "kind": "tour", "id": "…", "at": "…" }],

  // بخش ۸. اینجا فقط شمارش می‌آید؛ خودِ چک‌ها در `checks.json` و
  // `pages/*.json` زندگی می‌کنند.
  "checks": { "universal": 7, "contracts": 12, "invariants": 3 },

  // بخش ۹. سنجهٔ «چقدر این پروژه را می‌شناسیم»
  "coverage": { "routesKnown": 0.7, "routesToured": 0.4, "questionsOpen": 3 }
}
```

## سه قاعده که همه‌چیزِ این بخش به آن‌ها بند است

**۱. هر بند می‌گوید از کجا آمده.** کلید `by` چهار مقدار دارد و ترتیبِ اعتماد
همین است:

| `by` | یعنی | اعتماد |
|---|---|---|
| `user` | کاربر خودش گفته | ۱۰۰ |
| `tour` | در گشتِ حاضرِ آدم دیده شده | ۷۰ |
| `source` | از کد استخراج شده، بی‌مدل | ۶۰ |
| `run` | در یک اجرای خودکار دیده شده | ۵۰ |
| `docs` | از مستندات آمده | ۴۰ |
| `model` | مدل حدس زده | ۲۰ — نشانه‌دار در رابط |

**چرا اجباری است:** بدون این، حدسِ یک مدلِ ارزان و جملهٔ خودِ کاربر یک وزن
پیدا می‌کنند، و شش ماه بعد هیچ‌کس نمی‌داند کدام کدام است.

**و چرا فقط `by`، بی‌عددِ جداگانه.** اول یک `confidence` هم ذخیره می‌شد.
نخستین آزمونِ واقعی خرابی‌اش را نشان داد: بندی که به `by: user` ترفیع گرفت،
`confidence: 0.2`ِ مدل را با خودش برد — پرونده هم‌زمان می‌گفت «کاربر گفته» و
«۲۰٪ مطمئنیم». حالا عدد از `by` مشتق می‌شود و ذخیره نمی‌شود.

**۲. تعارض پنهان نمی‌شود.** اگر گشت چیزی ببیند که با بندِ `user` نمی‌خواند،
بند عوض **نمی‌شود** — یک `conflict` کنارش می‌نشیند و رابط نشانش می‌دهد.
بازنویسیِ خاموشِ حرفِ کاربر با حدسِ مدل، بدترین شکستِ ممکنِ این ساختار است.

**۳. شناخت کهنه می‌شود و باید بداند.** هر `pages/<slug>.json` یک
`domSignature` دارد (همان `src/steps/signature.js` که کش امروز استفاده
می‌کند). اجرای بعدی که آن صفحه را ببیند و امضا نخورد، صفحه `stale: true`
می‌گیرد. شناختِ کهنهٔ بی‌علامت از نبودِ شناخت بدتر است.

## کارها

- [x] `src/knowledge/store.js` — خواندن/نوشتن/اعتبارسنجیِ پرونده. محصور به
      `knowledge/`، همان الگوی `ui/src/lib/server/paths.js`.
- [x] `src/knowledge/schema.js` — شکلِ بالا، با `assertDossier()`. پروندهٔ
      خراب باید همان لحظه بشکند نه وقتی مدل چیز عجیبی تحویل داد.
- [x] `src/knowledge/render.js` — `dossier.json` → `dossier.md` برای خواندن.
- [x] رابط: `/projects/<کلید>/knowledge` — نمایش، نشانِ `by`، فهرست تعارض‌ها،
      پاسخ به `openQuestions`، چک‌ها، fixtureها، و تاریخچه
- [x] `userbug knowledge <پروژه>` در CLI (`--json`، `--history`، `--questions`، `--answer`). قاعدهٔ پروژه: هر کاری
      که رابط می‌کند از CLI هم بشود.
- [x] `knowledge/*/credentials.json` و `knowledge/*/tours/` به `.gitignore`.

---

# بخش ۲ — هضم سورس و مصاحبه

این را **اول** می‌سازیم چون مرورگر نمی‌خواهد، و بیشترِ `routes` و `stack` را
بدون یک فراخوانی مدل می‌دهد.

## آنچه بدون مدل به دست می‌آید

`src/source-access.js` همین حالا `listSourceFiles` و `readSourceFile`ِ محصور
دارد. روی همان‌ها:

- **استک** از `package.json` / `composer.json` / `requirements.txt`.
  (`.json` در `SOURCE_EXTENSIONS` هست، پس محصورسازی دست‌نخورده می‌ماند.)
- **روت‌ها** به‌شکل قاعده‌ای، نه مدلی:
  - SvelteKit: `src/routes/**/+page.svelte` → مسیر، با `[param]` و `(group)`
  - Next: `app/**/page.*` یا `pages/**`
  - Vue/React Router: الگوی `path:` در فایل روتر
  - PHP/Laravel: `routes/*.php`
  - هیچ‌کدام نخورد: خالی می‌ماند و گشت پرش می‌کند.
- **مستندات**: `README.md` و `docs/**` تا سقفِ حجم.

**چرا قاعده و نه مدل:** فهرست روت یک فکتِ ساختاری است. سپردنش به مدل یعنی
پول دادن برای چیزی که `readdir` جواب می‌دهد، و جواب هم گاهی غلط.

## آنچه مدل می‌کند — یک فراخوانی، نقشِ `analyze`

ورودی: استک + فهرست روت + سرِ فایل‌های روت + README.
خروجی: `summary`، `auth`، `entities`، `glossary`، `risks`، و مهم‌تر از همه
**`openQuestions`**.

**`openQuestions` چرا هستهٔ این بخش است:** مدل روی سورس نمی‌تواند بفهمد «حساب
تستی کدام است»، «کدام دکمه در محیط توسعه خطرناک است»، «این پروژه اصلاً برای چه
ساخته شده». اگر بگذاریم حدس بزند، حدسش با برچسب `model` وارد پرونده می‌شود و
می‌ماند. اگر بگذاریم بپرسد، جواب با برچسب `user` می‌آید. **پرسیدن ارزان‌تر از
حدس زدن است.**

## کارها

- [x] `src/knowledge/digest.js` — پیمایش، تشخیص استک، کشفِ روت.
- [x] `src/knowledge/routes.js` — شش آشکارساز، هرکدام با تست، هرکدام یک
      تابعِ خالص با تست در `scenarios/_selftest/`.
- [x] پرسش و پاسخ — در `digest.js` و `merge.js` (`answerQuestion`)
- [x] `userbug learn <پروژه>` با `--dry` · `POST /api/knowledge`
- [x] رابط: پرسش‌های بی‌جواب با جعبهٔ پاسخ، بالای صفحهٔ شناخت

---

# بخش ۳ — تزریقِ شناخت به مسیرهای موجود

بدون این بخش، بخش ۱ و ۲ فقط یک فایلِ قشنگ‌اند.

| کجا | چه می‌گیرد | چه چیزی درست می‌شود |
|---|---|---|
| `from-text.js` → `buildUser()` | روت‌ها، واژه‌نامه، `auth` | `go:` و برچسب‌ها دیگر حدس نیستند |
| `explore.js` → prompt | هدفِ صفحهٔ فعلی + `risks` | ده قدمِ سوخته روی فرضِ غلط |
| `do.js` → `askModel()` | واژه‌نامه | «سند» و «فایل» با هم قاطی نمی‌شوند |
| `target.js` → `explore.avoid` | `risks` ادغام می‌شود | فهرست دستی لازم نیست |

## قاعدهٔ بودجه

پرونده می‌تواند بزرگ شود و هر فراخوانی را گران کند. پس یک تابعِ **انتخاب**، نه
یک `JSON.stringify`:

```
knowledgeFor({ dossier, text, url, budget = 1200 })
```

- روت‌های مرتبط با `url` یا واژه‌های `text` (همان `keywords()` موجود)
- واژه‌نامه فقط برای واژه‌هایی که در متن آمده
- `auth` و `summary` همیشه (کوچک‌اند و همه‌جا لازم)
- `risks` همیشه (ایمنی، نه کیفیت)

**چرا سقف از روزِ اول:** اگر بعداً اضافه شود، تا آن روز چند سناریو با پرونده‌ای
که نصفش بی‌ربط بوده ساخته شده‌اند و کسی نمی‌فهمد چرا کیفیت افت کرده.

## کارها

- [x] `src/knowledge/select.js` — `knowledgeFor()` با سقفِ بودجه
- [x] وصل به سه مصرف‌کننده، با تستِ انتخاب و سقف
- [x] ادغام `risks` در `explore.avoid` (در خودِ `explore.js`، نه `loadTarget`)
- [ ] در `run.json` ثبت شود که کدام نسخهٔ پرونده استفاده شده — وگرنه «چرا این
      اجرا فرق داشت» جواب ندارد.

---

# بخش ۴ — گشتِ زنده

**بزرگ‌ترین تکه، و همان چیزی که کاربر خواسته:** مرورگر باز شود، خودِ کاربر
لاگین کند، منوها را بگردد، و هر جا خواست بگوید «این صفحه برای این است».

## تصمیمِ معماری: گشت زیر `playwright test` نمی‌رود

اجرای تست، چرخهٔ عمرِ خودش را دارد: timeout، ترتیب، و بستنِ مرورگر در پایان.
گشت دقیقه‌ای تا ساعتی طول می‌کشد و **سرعتش را آدم تعیین می‌کند**. جا دادنش در
`test()` یعنی جنگیدن با هر مکانیزمِ ایمنیِ آن چارچوب.

پس: یک پروسهٔ نودِ مستقل که `chromium.launchPersistentContext` را باز می‌کند
(headed)، و همان چیزهایی را سوار می‌کند که fixture سوار می‌کرد.

**چه چیزی بازاستفاده می‌شود — تقریباً همه‌چیز:**

| از | برای |
|---|---|
| `INIT_SCRIPT` + `attachClientObservers` | همان رخدادها. باگی که کاربر حین گشت ببیند، **یافتهٔ واقعی** است |
| `createServerCollectors` + `startAll` | لاگ سرور، هم‌زمان با گشت |
| `judge()` از `oracle.js` | داوری، بی‌تغییر |
| `RunStore` | گشت هم یک اجراست: `runs/<id>/` با رخداد و عکس |
| `snapshotPage` / `descriptorFor` | عکسِ دسترسی‌پذیریِ صفحه، با همان شکلِ توصیف‌گرِ کش |
| `jobs.js` + SSE | پنل کنترل، مثل هر اجرای دیگر |

**چه چیزی تازه است:** ضبط‌کننده، پنلِ علامت‌گذاری، و خروجی‌گیری.

## پنل کنترل کجاست — نه در اپ

کاربر گفته «توی برنامهٔ ما تایپ کند». و همین درست است. تزریقِ یک HUD به صفحهٔ
اپ، دقیقاً همان کاری است که این پروژه در جای دیگر با دقت از آن پرهیز می‌کند
(پنجرهٔ مزاحمِ نپی که ثبت شد و خاموش شد). ابزاری که برای کشفِ تداخل ساخته شده،
خودش نباید تداخل بسازد.

پس: پنجرهٔ مرورگر باز است، و کنارش `/projects/<کلید>/tour` در رابط، که از راه
SSE زنده است:

- آدرسِ فعلی، عنوان، و بندانگشتیِ صفحه (هر ناوبری + هر ۵ ثانیه)
- «ثبتِ این صفحه» → عکس + snapshot + **جعبهٔ متنِ «این صفحه برای چیست؟»**
- «شروع/توقف ضبطِ قدم‌ها»
- فهرستِ قدم‌های ضبط‌شده، قابلِ حذفِ تکی
- یافته‌های همین لحظه (از داور)
- «پایان گشت»

## ضبط‌کننده

اسکریپتِ init با شنوندهٔ فاز-capture روی `click`، `change`، `submit`، و
`keydown`ِ Enter. هر رخداد از راه `page.exposeBinding('__ubRecord')` بیرون
می‌آید.

**توصیف‌گر، نه سلکتور.** ضبط‌کننده‌های معمولی `div>div:nth-child(3)` می‌نویسند
که فردا می‌شکند. اینجا همان چیزی نوشته می‌شود که `snapshot.js` می‌سازد:
`{role, name, label, exact, nth}`. نود پس از دریافت، با
`getByRole(...).count()` اعتبارش را می‌سنجد و در صورت ابهام `nth` اضافه
می‌کند — دقیقاً کاری که `descriptorFor` می‌کند.

**رمز از صفحه بیرون نمی‌آید.** `input[type=password]` مقدارش جای `value` یک
`{secret: true}` می‌فرستد. این قاعده در خودِ اسکریپتِ داخلِ صفحه است، نه در
نود: چیزی که فرستاده نشود، لو هم نمی‌رود.

## چهار خروجیِ یک گشت

**۱. صفحه‌ها** → `knowledge/<کلید>/pages/*.json`، با جملهٔ خودِ کاربر و
`by: "user"`.

**۲. سناریوی قابل اجرا** → `scenarios/<کلید>/_drafts/tour-<slug>.yml`.
قدم‌های ضبط‌شده به فعل‌های موجود ترجمه می‌شوند: `go`، `click`، `fill`، `press`.
**و بلافاصله بازپخش می‌شود.** پیش‌نویسی که یک بار اجرا نشده، سناریو نیست؛ همان
قاعدهٔ «یافته بدون بازتولید، یافته نیست» یک پله بالاتر. `status: draft` می‌ماند
تا آدم ببیندش.

**۳. کشِ آموخته** → `scenarios/<کلید>/_learned/*.json`.
اینجاست که گشت هزینهٔ خودش را برمی‌گرداند: کاربر یک بار روی «ورود» کلیک کرده و
گفته این «دکمهٔ ورود» است → یک `_learned` با `resolvedBy: "human"` و
`healCount: 0`. هر `do: دکمهٔ ورود را بزن` از آن به بعد **بدون مدل** حل
می‌شود. کش امروز با پولِ مدل پر می‌شود؛ این آن را با وقتِ آدم پر می‌کند — که
یک بار داده می‌شود.

**۴. یافته‌ها** → مسیرِ همیشگی. تریاژ نمی‌داند این‌ها از گشت آمده‌اند و لازم هم
ندارد بداند.

## سناریوی لندینگ

نتیجهٔ نخستین گشتِ هر پروژه یک سناریوی نامدار می‌شود:
`scenarios/<کلید>/آشنایی.yml` — ورود، و سری به صفحه‌های اصلی، با `expect` فقط
جایی که کاربر تأیید کرده.

دو نقش دارد: **دودسنجِ** پروژه (اگر این بشکند، بقیه بی‌معنی است) و **نقشهٔ
زنده** (چون هر بار اجرا می‌شود، کهنه شدنِ شناخت را خودش لو می‌دهد).

## کارها

- [x] `src/tour/session.js` — مرورگرِ حاضر + همهٔ رصدگرها + چک‌ها
- [x] `src/tour/recorder.js` — اسکریپتِ داخلِ صفحه + `descriptorFor` در نود
- [x] `src/tour/emit.js` — گشت → صفحه‌ها، YAML، کش، پرونده
- [x] `ui/src/lib/server/tours.js` — گشت شیءِ زنده است، نه job؛ مهلتِ بی‌فعالیتی ۳۰ دقیقه
- [x] رابط: `/projects/<کلید>/tour` با SSE زنده
- [x] `userbug tour <پروژه>` در CLI، با توضیح از stdin
- [x] خودآزما: ۱۳ تست، شاملِ ضبط → YAML → **بازپخش**

---

# بخش ۵ — حساب‌های ذخیره‌شده

امروز `identity` فقط `fresh-signup` است: هر اجرا یک کاربرِ تصادفی. برای اپی که
ثبت‌نامش باز نیست، یا برای آزمودنِ حسابی که داده دارد، این کافی نیست.

## شکل

`knowledge/<کلید>/credentials.json`، در `.gitignore`:

```jsonc
{
  "accounts": [
    { "id": "admin", "email": "a@b.c", "passwordEnv": "UB_NEPI_ADMIN_PASSWORD", "note": "…" },
    { "id": "guest", "email": "g@b.c", "password": "…" }
  ]
}
```

**پیش‌فرض `passwordEnv` است، نه `password`.** این پروژه حاضر نیست `.env` را
بخواند تا به مدل بدهد (`SECRET_PATTERNS`)؛ نوشتنِ رمزِ متنی روی دیسک با همان
موضع نمی‌خواند. ذخیرهٔ متنی ممکن است، ولی با انتخابِ صریحِ کاربر و هشدارِ
روشن.

## قواعد

- در سناریو با `{{account.admin.email}}` — فضای‌نامِ تازه کنار `identity`.
- `secretsOf()` در `redact.js` باید رمزِ حساب‌ها را هم برگرداند، وگرنه اولین
  snapshot که به مدل می‌رود آن را با خودش می‌برد.
- روی `environment: production` ذخیره **رد می‌شود**، مگر با تأییدِ جداگانه.
  همان دروازه‌ای که `guard.js` برای قلاب‌های مخرب دارد.
- پیشنهادِ ذخیره فقط **بعد از** گشت می‌آید، وقتی ضبط‌کننده فرمِ رمز دیده:
  «این حساب برای سناریوهای بعدی ذخیره شود؟»

## کارها

- [x] `src/knowledge/credentials.js` — خواندن، حلِ `passwordEnv`، دروازهٔ محیط
- [x] `interpolate.js`: فضای‌نامِ `account`
- [x] `redact.js`: `secretsOf(identity, extra)` — و هر دو مصرف‌کننده وصل شدند
- [x] رابط: مدیریتِ حساب‌ها در صفحهٔ شناخت؛ رمز هرگز به کلاینت برنمی‌گردد ·
      `userbug accounts` در CLI

---

# بخش ۶ — فایل: آپلود و دانلود

کاربر مشخصاً پرسید. وضعیت امروز نامتقارن است.

## دانلود — هست ولی ناقص

`src/scenario/run.js:396` فعل `download` را دارد، ولی:

```js
const text = await fs.readFile(await download.path(), 'utf8');
```

**هر فایلِ غیرمتنی اینجا خراب می‌شود.** PDF، ZIP، تصویر — همه به‌شکل رشتهٔ
مخدوش در متغیر می‌نشینند، بی‌آنکه چیزی بشکند. یعنی سناریویی که PDF دانلود
می‌کند سبز می‌شود و هیچ نمی‌گوید.

**درست:** فایل در `runs/<id>/downloads/` ذخیره شود و متغیر
`{path, filename, size, mime}` بگیرد. رمزگشاییِ متنی فقط با درخواستِ صریح
(`as: text`). و `expect: { download: { filename: /…/, minSize: 1000 } }`.

## آپلود — نیست

فعلی وجود ندارد. دو مسیر لازم است، چون اپ‌ها هر دو را دارند:

```yaml
- upload:
    to: { label: "انتخاب فایل" }     # input[type=file] مستقیم
    file: "fixtures/sample.pdf"

- upload:
    trigger: { role: button, name: "بارگذاری" }   # از راه fileChooser
    file: "fixtures/sample.pdf"
```

**فایل‌ها فقط از `knowledge/<کلید>/fixtures/`.** همان چهار قاعدهٔ
`source-access.js`: بدون اعلام خبری نیست، بیرون از ریشه هرگز، symlink بررسی
می‌شود. سناریویی که بتواند هر مسیری از دیسک را آپلود کند، یک درِ باز است — و
سناریو را ممکن است مدل نوشته باشد.

## در گشت

- ضبط‌کننده رخدادِ `filechooser` را می‌بیند و می‌پرسد: «این فایل در fixtures
  کپی شود تا سناریو تکرارپذیر شود؟» بدون این، قدمِ ضبط‌شده به فایلی روی دیسکِ
  کاربر اشاره می‌کند و روی هر ماشین دیگری می‌شکند.
- هر دانلودی که حین گشت رخ دهد، در `dossier.files.downloads` ثبت می‌شود.

## کارها

- [x] `run.js`: بازنویسی `download`، افزودن `upload`.
- [x] `verbs.js`: ثبت `upload`
- [x] `src/knowledge/fixtures.js` — دسترسیِ محصور به پوشهٔ فایل‌ها
- [x] `expect: { download: … }` — اندازه، نام، و فایلِ خالی
- [x] رابط: فهرست fixtureها در صفحهٔ شناخت (بارگذاری از مرورگر هنوز نه)
- [x] خودآزما: `scenarios/_selftest/files.spec.js` — ۱۱ تست

---

# بخش ۷ — مستندات

کوچک، و آخر، چون بی‌بقیه بی‌معنی است.

- فایل‌های `docs/**` از همان محصورسازیِ سورس (هست).
- URL: کاربر آدرسِ مستندات را می‌دهد → واکشی، تبدیل به متن، ذخیره در
  `knowledge/<کلید>/docs/`. **با تأییدِ صریح**، چون یک درخواستِ شبکه به مقصدی
  است که کاربر داده.
- چیزی که از URL می‌آید `by: "docs"` می‌گیرد و **هرگز `user` نمی‌شود**. متنِ
  یک صفحهٔ وب داده است، نه دستور.

---

# بخش ۸ — سنجهٔ هوشمند

## شکافِ امروز، بی‌تعارف

`oracle.js` فقط **رخداد** را داوری می‌کند: خطای کنسول، `pageerror`، درخواستِ
شکست‌خورده، ۵۰۰، خطِ لاگ سرور. یعنی این ابزار امروز می‌بیند، ولی **انتظار
ندارد**. سه چیز از همین جا رد می‌شوند:

- صفحه‌ای که کاملاً سفید رندر شود و هیچ خطایی ندهد → **سبز**
- ذخیره‌ای که روی صفحه موفق نشان دهد و در دیتابیس ننشیند → **سبز**
- دکمه‌ای که کلیک شود و هیچ اتفاقی نیفتد → **سبز**

هر انتظاری امروز باید دستی در سناریو نوشته شود (`expect` / `assert`). یعنی
کیفیتِ تست به حوصلهٔ کسی بند است که سناریو را نوشته. این همان جایی است که
«شناخت» به کار می‌آید: **چیزی که پرونده می‌داند، می‌تواند خودش را بسنجد.**

## سه لایه، به ترتیبِ منبعِ حقیقت

| لایه | از کجا می‌آید | مدل لازم دارد؟ | چه چیزی می‌گیرد |
|---|---|---|---|
| ۱ — همگانی | هیچ. برای هر اپی درست است | ❌ | صفحهٔ سفید، متنِ خامِ خطا، کنشِ بی‌اثر |
| ۲ — قراردادِ صفحه | گشت و اجراهای قبلی | ❌ | ناپدید شدنِ چیزی که قبلاً بود |
| ۳ — ناوردا | سورس و کاربر | کم | باگِ منطقی |

### لایهٔ ۱ — چکِ همگانی

بی‌مدل، قطعی، و بعد از هر ناوبری و هر قدم اجرا می‌شود. فهرستِ آغازین:

- **صفحه خالی نیست** — کمتر از N عنصرِ قابل‌تعامل یا کمتر از M نویسه متن
- **عنوان سالم است** — `<title>` خالی نیست، و «404»/«Error»/«Not Found» نیست
- **متنِ خامِ خطا روی صفحه نیست** — `undefined`، `NaN`، `[object Object]`،
  `null`، خطِ stack trace، `{{…}}`ِ جای‌گذاری‌نشده
- **کنش بی‌اثر نبوده** — بعد از کلیک، یا `domSignature` عوض شده، یا آدرس، یا
  درخواستی رفته. هر سه ثابت یعنی دکمه کار نکرد
- **فرمِ ارسال‌شده ساکت نمانده** — یا ناوبری، یا پیام، یا تغییر
- **صفحه اسکرول افقی ناخواسته ندارد** (RTL این را زیاد می‌شکند)

**چرا این‌ها نه بیشتر:** هر چکِ همگانی روی *همهٔ* پروژه‌ها اجرا می‌شود، پس یک
چکِ پرسروصدا کلِ گزارش را بی‌ارزش می‌کند. فهرست کوتاه می‌ماند و هر افزوده باید
روی نپی و `userbug-ui` هر دو ساکت باشد.

**قابل خاموش کردن، با دلیل.** در `checks.json` هر چک می‌تواند `off` شود، ولی
کلید `why` اجباری است — همان قاعدهٔ `allowlist` که در README نوشته شده
«فهرست بلند یعنی داریم مشکل را زیر فرش می‌کنیم».

### لایهٔ ۲ — قراردادِ صفحه

از گشت می‌آید و در `pages/<slug>.json` می‌نشیند:

```jsonc
{
  "path": "/library",
  "purpose": "…",              // جملهٔ کاربر
  "domSignature": "8a6a423a",
  "contract": {
    "mode": "watch",           // watch | expect | off
    "must": [
      { "role": "heading", "name": "کتابخانه" },
      { "role": "button",  "name": "سند جدید" }
    ],
    "seenIn": 12, "lastSeen": "…", "approvedBy": null
  }
}
```

**تفاوتِ `watch` و `expect` تمامِ ماجراست.** قرارداد از رفتارِ *فعلی* اپ ضبط
می‌شود، پس اگر مستقیم `expect` شود، **باگِ امروز را رسمی می‌کند** — دقیقاً
همان تفاوتی که `from-text.js` بین خودش و `--author` نوشته است. پس:

- `watch` (پیش‌فرض): نبودنش یافته ثبت می‌کند، سناریو را نمی‌شکند
- `expect`: فقط با تأییدِ آدم در رابط. از آن به بعد شکستِ سخت است
- ارتقا از `watch` به `expect` پیشنهاد می‌شود وقتی `seenIn` از آستانه رد شد
  (مثلاً ۱۰ اجرای متوالی) — پیشنهاد، نه خودکار

### لایهٔ ۳ — ناوردا

جایی که باگِ منطقی گیر می‌افتد. `TODO.md` این را زیر «استخراج invariant از سورس»
باز گذاشته بود؛ اینجا جایش است.

```jsonc
{
  "id": "email-unique",
  "statement": "دو کاربر با یک ایمیل نمی‌توانند وجود داشته باشند",
  "by": "source",
  "from": "schema.sql:14 — UNIQUE(email)",
  "check": { "query": "SELECT COUNT(*) c FROM users WHERE email = ?", "max": 1 },
  "mode": "watch"
}
```

دو منبع:

- **از سورس، بی‌مدل:** `UNIQUE`، `NOT NULL`، `CHECK`، `FOREIGN KEY` در فایل
  schema؛ قواعدِ اعتبارسنجی (طول کمینه، regex، مقادیر enum). این‌ها با الگو
  استخراج می‌شوند، نه با حدس.
- **از کاربر، در مصاحبه:** یک پرسشِ ثابت — «چه چیزی در این سامانه **هرگز**
  نباید اتفاق بیفتد؟» جوابش `by: user` می‌گیرد و از همه معتبرتر است.

**اجرا:** با فعل‌های موجود — `query` (اگر `state.sql` اعلام شده) و `request`.
پروژه‌ای که هیچ‌کدام را ندارد ناوردا نمی‌گیرد، و این باید صریح گفته شود؛ همان
تفکیکی که README برای «جعبه‌سیاه» گذاشته.

## یافته، نه پرتاب

هیچ چکی `throw` نمی‌کند. همه از همان لولهٔ `findings` رد می‌شوند با
`source: 'check'` و شناسهٔ چک. **چرا:** اگر چکِ اول سناریو را بشکند، چهار
قراردادِ شکستهٔ بعدی هرگز دیده نمی‌شوند و اجرای بعدی همان یکی را دوباره پیدا
می‌کند. `expect`ِ تأییدشده تنها استثناست.

هر یافتهٔ چک شناسهٔ چکِ سازنده‌اش را حمل می‌کند — بدون آن، حلقهٔ بخش ۹ نمی‌تواند
چکِ پرسروصدا را پیدا کند.

## کارها

- [x] `src/checks/universal.js` — یک `evaluate`، ده چک، همه با تستِ دوسویه
- [x] `src/checks/run.js` — اجراگر و تبدیل به یافته
- [x] وصل به `fixtures.js` — همهٔ مسیرها از `ub.step` رد می‌شوند، پس کاوش هم پوشش دارد
- [ ] `src/checks/contract.js` — ساخت قرارداد از snapshot، سنجش، ارتقای
      پیشنهادی.
- [ ] `src/checks/invariant.js` + `src/knowledge/schema-mine.js` — استخراج از
      SQL و فایل‌های اعتبارسنجی.
- [x] `knowledge/<کلید>/checks.json` با `mode` و `why` و شمارندهٔ سروصدا · `userbug checks`
- [ ] رابط: صفحهٔ چک‌ها — روشن/خاموش، `watch`→`expect`، و آمارِ سروصدا.
- [ ] خودآزما: صفحه‌ای که عمداً سفید است، صفحه‌ای با `[object Object]`، و
      دکمه‌ای بی‌اثر. هر سه باید گرفته شوند و روی نپی هیچ‌کدام قلابی ندهند.

---

# بخش ۹ — حلقهٔ یادگیری: چطور به مرور بهتر می‌شود

بخش‌های قبل شناخت را **می‌سازند**. این بخش تضمین می‌کند که شناخت **رشد کند** و
نپوسد. بدون این، پروندهٔ روزِ اول تا ابد پروندهٔ روزِ اول می‌ماند.

## پنج مسیرِ بازخورد

**۱. هر اجرا پرونده را تغذیه می‌کند.**
آدرسی که دیده شد و در `routes` نبود → افزوده می‌شود با `by: run`، و یک پرسش
می‌سازد: «این صفحه برای چیست؟». صفحه‌ای که
`domSignature`اش نخورد → `stale: true`. هیچ‌کدام مدل نمی‌خواهد.

**۲. تریاژ به شناخت برمی‌گردد — مهم‌ترین حلقه.**
`triage/` امروز وجود دارد ولی یک‌طرفه است: یافته می‌آید، آدم برچسب می‌زند،
تمام. برچسب باید **برگردد**:

| برچسبِ آدم | چه چیزی در شناخت عوض می‌شود |
|---|---|
| قلابی | `falsePositives++` روی چکِ سازنده. از آستانه رد شد → خودکار `off` و در رابط علامت‌دار |
| باگِ واقعی | به `risks` می‌رود، و یک **ناوردا** پیشنهاد می‌شود تا رگرسیونش گرفته شود |
| رفتار درست است | به `contract.must` همان صفحه اضافه می‌شود |
| بعداً | دست‌نخورده |

**چرا این حلقه از بقیه مهم‌تر است:** تنها جایی است که *قضاوتِ آدم* وارد سیستم
می‌شود. بقیهٔ مسیرها داده جمع می‌کنند؛ این یکی معنا اضافه می‌کند.

**۳. کش سلامتِ خودش را گزارش می‌کند.**
`healCount` همین حالا در `_learned/*.json` نوشته می‌شود و **هیچ‌کس نمی‌خواندش**.
`healCount` بالا روی یک قدم یعنی آن صفحه مدام عوض می‌شود → صفحه `stale` و در
رابط دیده می‌شود. داده‌اش هست؛ فقط مصرف‌کننده ندارد.

**۴. پرسش‌ها کم می‌شوند.**
`openQuestions` که جواب بگیرد `by: user` می‌شود و دیگر پرسیده نمی‌شود. جواب
نگرفته بماند، بعد از N اجرا دوباره بالا می‌آید — ولی هرگز حدس جایش را
نمی‌گیرد.

**۵. سنجهٔ شناخت، یک عدد.**
در صفحهٔ پروژه: چند درصدِ روت‌ها `purpose` دارند، چند صفحه قرارداد دارند، چند
پرسش باز است، آخرین گشت کِی بود.

**چرا یک عدد لازم است:** بدون سنجه، «به مرور بهتر می‌شود» یک ادعاست نه یک
واقعیت. این عدد باید بالا برود؛ اگر نرفت، این طرح شکست خورده و باید بدانیم.

## تاریخچه — چون رشد باید قابل بازبینی باشد

`knowledge/<کلید>/history.ndjson`، یک خط به ازای هر تغییرِ شناخت:

```jsonc
{ "at": "…", "op": "add", "path": "routes[/settings]", "by": "tour", "tour": "…" }
{ "at": "…", "op": "demote", "path": "checks[empty-page]", "by": "triage", "why": "۴ یافتهٔ قلابی" }
```

همان قاعدهٔ `by` یک پله بالاتر: پرونده می‌گوید **الان** چه می‌داند، تاریخچه
می‌گوید **چطور** به اینجا رسید. بدون آن، «چرا این چک خاموش است؟» جواب ندارد.

## قاعده‌ای که نباید شکسته شود

**هیچ حلقهٔ خودکاری حرفِ `by: user` را عوض نمی‌کند.** خودکارها می‌توانند
اضافه کنند، علامت بزنند، پیشنهاد بدهند، و چکِ خودشان را خاموش کنند. عوض کردنِ
چیزی که آدم گفته، فقط با خودِ آدم. سیستمی که به‌مرور یاد می‌گیرد، به‌مرور هم
می‌تواند اشتباهِ خودش را روی حقیقت بنویسد — و آن لحظه دیگر هیچ‌کس به پرونده
اعتماد نمی‌کند.

## کارها

- [x] `src/knowledge/history.js` — نوشتن و خواندنِ `history.ndjson`
- [x] `src/knowledge/absorb.js` — در `finalize.js` صدا زده می‌شود
- [x] وصل تریاژ به شناخت: `src/knowledge/feedback.js` و کشویی «قضاوت» در تریاژ
- [x] `healCount` بالای آستانه در خلاصهٔ اجرا گزارش می‌شود · [ ] کهنگیِ خودکارِ صفحه (نیازمند ثبتِ امضای صفحه در اجرا)
- [x] `src/knowledge/coverage.js` + کارتِ سنجه در صفحهٔ شناخت
- [x] `userbug knowledge <پروژه> --history`

---

# ترتیب کار

هر قدم خودش قابل استفاده است — همان قاعده‌ای که مسیرِ فازهای قبلی داشت.

| # | قدم | بخش | چرا این ترتیب |
|---|---|---|---|
| ۱ | پروندهٔ شناخت: انبار، شکل، تاریخچه، رابطِ نمایش | ۱ | بقیه جایی برای نوشتن ندارند |
| ۲ | هضم سورس + مصاحبه | ۲ | بی‌مرورگر، و بیشترِ `routes` را رایگان می‌دهد |
| ۳ | **چکِ همگانی** | ۸ لایهٔ ۱ | به شناخت وابسته نیست و همین امروز صفحهٔ سفید را می‌گیرد. ارزانْ‌ترین برد |
| ۴ | تزریق به `from-text` / `explore` / `do` | ۳ | **نقطهٔ تصمیم.** اگر کیفیت بالا نرفت، بقیه را نساز |
| ۵ | آپلود و دانلود | ۶ | مستقل، و گشت به آپلودِ درست نیاز دارد |
| ۶ | گشتِ زنده + ضبط + قراردادِ صفحه | ۴ و ۸ لایهٔ ۲ | بزرگ‌ترین. قرارداد از خروجیِ همان گشت می‌آید، پس با هم |
| ۷ | حساب‌های ذخیره‌شده | ۵ | پیشنهادش از دلِ گشت می‌آید |
| ۸ | ناوردا از سورس و مصاحبه | ۸ لایهٔ ۳ | باگِ منطقی. آخر از چک‌ها چون سخت‌ترین و پرخطرترینِ سه لایه است |
| ۹ | حلقهٔ یادگیری: تریاژ ← شناخت، سنجه | ۹ | مصرف‌کنندهٔ همهٔ بالایی‌ها. زودتر ساختنش یعنی حلقه‌ای که چیزی برای بستن ندارد |
| ۱۰ | مستندات | ۷ | تکمیلی |

سه قدم **پیوسته**‌اند و منتظر نوبت نمی‌مانند، چون هرکدام تکه‌ای‌اند که کنارِ
همان بخشِ مربوطه ساخته می‌شوند: نوشتن در `history.ndjson` (از قدم ۱ به بعد،
هر تغییرِ شناخت)، جذبِ روتِ تازه در `finalize.js` (از قدم ۳)، و شمردنِ سروصدای
هر چک (از همان روزی که نخستین چک ساخته شد).

**قدم ۴ نقطهٔ تصمیم است.** اگر سناریوی ساخته‌شده با پرونده از سناریوی بدون
پرونده بهتر نبود، مسئله جای دیگری است و گشتِ زنده حلش نمی‌کند.

---

# هفت اصطکاکی که پنهان نمی‌شوند

**۱. گشت یک پروسهٔ بی‌پایان است و `jobs.js` برای اجرای پایان‌دار ساخته شده.**
امروز هر job یک اجرای تست است که تمام می‌شود. گشت تمام نمی‌شود مگر کاربر
بگوید. وضعیتِ `attended` لازم است، و پاسخِ روشن به «اگر کاربر تبِ رابط را
ببندد چه؟» — پیشنهاد: مرورگر باز می‌ماند و job زنده، ولی یک مهلتِ بی‌فعالیتی
(مثلاً ۳۰ دقیقه) می‌بنددش. مرورگرِ فراموش‌شده روی ماشینِ کاربر بدترین حالت
است.

**۲. ضبطِ کلیک، سناریوی خوب نمی‌سازد.** ضبط‌کننده‌ها همه‌جا شکننده‌اند. سه
محافظ: توصیف‌گرِ معنایی به‌جای سلکتور، اعتبارسنجی همان لحظه در نود، و بازپخشِ
اجباری پیش از ذخیره. با این سه، خروجی هنوز `draft` است — نه چون بی‌اعتماد
است، چون آدم باید ببیندش.

**۳. پرونده به مدل می‌رود.** همان مرزِ اعتمادی که `source-access.js` با چهار
قاعده حفظش کرد. پرونده هم محتوای اپ دارد و هم جملهٔ کاربر. پس: `redactDeep`
پیش از هر ارسال، و در رابط صریح نوشته شود «این بخش به مدل فرستاده می‌شود».

**۴. شناخت کهنه می‌شود.** پاسخ در بخش ۱ آمد (`domSignature` + `stale`)، ولی یک
تصمیمِ باز مانده: صفحهٔ کهنه چه کند؟ پیشنهاد: هیچ — فقط علامت بخورد و در رابط
دیده شود. حذفِ خودکارِ شناختِ کهنه یعنی از دست دادنِ چیزی که آدم وقت گذاشته.

**۵. گشت باید روی محیطِ درست باشد.** کاربر با «اکانت واقعی» لاگین می‌کند و
ممکن است روی تولید باشد. `guard.js` امروز فقط جلوی قلاب‌های مخرب را می‌گیرد.
گشت روی `production` باید ممکن باشد (خواندنی است) ولی: بدون ریست، بدون قلاب،
با هشدارِ دائمی در پنل، و ذخیرهٔ اعتبار فقط با تأییدِ جدا.

**۶. چکِ هوشمند می‌تواند گزارش را خفه کند.** چکی که روی هر صفحه اجرا شود و ۵٪
خطا بدهد، در اجرایی با ۴۰ صفحه دو یافتهٔ قلابی می‌سازد — و دو یافتهٔ قلابی در
هر اجرا یعنی سه هفته بعد کسی گزارش را نمی‌خواند. سه محافظ: `watch` پیش‌فرض
است نه `expect`؛ هر چک شناسه دارد و سروصدایش شمرده می‌شود؛ و چکی که از آستانهٔ
قلابی رد شود خودش خاموش می‌شود. **این تنها جایی است که خودکار اجازه دارد چیزی
را خاموش کند** — چون سکوتِ یک چکِ بد، از سکوتِ کلِ گزارش کم‌هزینه‌تر است.

**۷. شناختِ خودافزا می‌تواند اشتباهِ خودش را تثبیت کند.** حلقهٔ بخش ۹ داده
اضافه می‌کند، و دادهٔ غلطی که یک بار وارد شد در اجرای بعدی مثل واقعیت خوانده
می‌شود. پاسخ همان قاعدهٔ آخرِ بخش ۹ است (`by: user` دست‌نخوردنی) به‌علاوهٔ
`history.ndjson` که هر افزوده را قابلِ پس گرفتن می‌کند. ولی این یک تضمینِ کامل
نیست و باید هر چند وقت با یک گشتِ تازه سنجیده شود.

---

# آنچه این طرح **نمی‌کند**

- **سناریوی داینامیک** را تعریف نمی‌کند. آن بندِ باز از فهرستِ ده‌گانه است و
  مسئله‌اش جداست.
- **گشتِ خودکارِ بی‌آدم** نمی‌سازد. آن همان `explore` است که هست. ارزشِ گشت
  دقیقاً در حضورِ آدم است — یک بار.
- **کانفیگ هدف را از پرونده نمی‌سازد.** پرونده به کانفیگ *پیشنهاد* می‌دهد
  (`risks` → `explore.avoid`)؛ نوشتنش با کاربر است.
- **چک، جای سناریو را نمی‌گیرد.** لایهٔ ۱ می‌گوید «صفحه سالم رندر شد»، لایهٔ ۲
  می‌گوید «چیزی که بود هنوز هست»، لایهٔ ۳ می‌گوید «قاعده نشکست». هیچ‌کدام
  نمی‌گوید «کاربر توانست کارش را بکند». آن هنوز کارِ سناریوست.
