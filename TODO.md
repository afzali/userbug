# userbug — فهرست ساخت

فهرست کارِ **خودِ برنامه**. یافته‌های اپ‌های تحت تست اینجا نیست — آن‌ها در
`findings/` می‌نشینند.

طرح کامل: [Artifact](https://claude.ai/code/artifact/8c2e8721-f2ec-4724-8aa2-65ffbf28582d)

**وضعیت:** فاز ۰ و ۱ بسته شدند. هستهٔ فاز ۲ کار می‌کند و معیار پایانش برآورده
شده: مدل مسیر را یاد می‌گیرد، ده اجرای بعدی بدون هیچ تماسی می‌روند، و تغییرِ
عمدی فقط همان یک قدم را heal می‌کند. مدل‌های پیش‌فرض رایگان‌اند.
کاوش آزاد، نویسندگیِ پیش‌نویس، بازتولیدِ خودکار و گزارش آمار مدل هم کار
می‌کنند. **فاز ۲ تمام است.**

**فاز ۳ ساخته شد و کارهایش سنجیده شده‌اند** — اجرای زنده، خط زمانی، تریاژ،
مقایسه، ویرایش فایل و Trace Viewer روی `127.0.0.1`. معیار پایانش («یک هفته
بدون ترمینال») ذاتاً زمان می‌خواهد و تا آن هفته نگذرد تیک نمی‌خورد. فاز ۴ شروع
شده: حالت CI بسته است.

---

## فاز ۰ — برش عمودی

اثبات اینکه زنجیره کار می‌کند. عمداً کوچک، عمداً بدون CLI و بدون AI.

- [x] ساخت پروژه، نصب `@playwright/test` و `@faker-js/faker` و chromium
- [x] `targets/nepi.config.js` — فقط آدرس و دسترسی، بدون دستور اجرا
- [x] `src/target.js` — بارگذاری هدف + دروازهٔ ایمنی `environment`
- [x] `playwright.config.js` — انتخاب دستگاه از `UB_DEVICE`
- [x] `src/global-setup.js` — هر اجرا پوشهٔ خودش
- [x] `src/observe/client.js` — console، pageerror، شبکه، ۴xx/۵xx، dialog
- [x] اسکریپت اولیه برای `unhandledrejection` (Playwright خودش نمی‌دهد)
- [x] `src/observe/server.js` — tail فایل با آفست، مقاوم به چرخش لاگ
- [x] `src/observe/oracle.js` — داور، allowlist، اثرانگشت، ادغام
- [x] `src/store/run-store.js` — `run.json` ، `events.ndjson` ، `findings.ndjson` ، `shots/`
- [x] `src/report/html.js` + `src/reporter.js` — گزارش تک‌فایلی
- [x] `src/fixtures.js` — fixture خودکار `auto: true` روی همهٔ تست‌ها
- [x] `ub.step()` — مرز زمانی هر قدم، عکس، و لاگ سرورِ همان بازه
- [x] `ub.note()` — یافتهٔ تشخیصِ سناریو
- [x] `ub.dismissBlockers()` — بستن پنجرهٔ مزاحم مثل کاربر واقعی، با ثبت
- [x] `ub.clearBrowserState()` — کوکی، localStorage، IndexedDB، cache، SW، **OPFS**
- [x] `src/data/persian.js` — هویت تازه + رشته‌های بدخیم فارسی
- [x] خودآزمای رصدگر — تزریق عمدی خطا، تا «صفر خطا» با «رصدگر خاموش» اشتباه نشود
- [x] اجرای دو دستگاهی: `desktop` و `Pixel 7`

**معیار پایان**

- [x] پنج باگ ناشناخته در نپی — کامل شد (نگاه: `findings/nepi.md`)

---

## فاز ۱ — موتور و قرارداد

هنوز بدون AI. اینجا چیزی که در فاز ۰ اسکریپت بود، برنامه می‌شود.

### CLI — `bin/userbug.js`

- [x] `run <target> [--scenario] [--grep] [--device] [--headed] [--repeat]`
- [x] `report <runId|latest>` — بازسازی گزارش از مخزن، بدون اجرای دوباره
- [x] `list` — فهرست اجراها با وضعیت و تعداد یافته
- [x] `diff <runA> <runB>` — چه یافته‌ای تازه است و چه یافته‌ای رفته
- [x] شناسهٔ اجرا با «latest» یا پیشوندِ یکتا
- [x] `replay <runId> [--only-findings]` — همان هدف، همان دستگاه، همان سناریوها
- [x] ثبت فهرست سناریوها در `run.json` تا `replay` بداند چه را دوباره ببرد
- [ ] ~~`resume <runId>`~~ — **کنار گذاشته شد.** ادامه از قدمی که ماند یعنی
      بازگرداندن نشست و دیتابیس محلی و کش به همان نقطه؛ مرورگر راهی برایش
      نمی‌دهد. به‌جایش `replay --only-findings` هست. اگر روزی لازم شد، راهش
      عکس‌برداری از وضعیت است نه ادامه دادن.
- [x] `--persona` و `--depth` — هر دو با همان ترتیب: پرچم خط فرمان بر سناریو
      می‌چربد، تا بشود یک سناریو را یک بار کم‌عمق برای دیدنِ مسیر و یک بار
      عمیق برای شکار اجرا کرد بدون دست زدن به فایل.

      عمق عدد است نه نامی مثل «کم/متوسط/زیاد»: هر قدمِ کاوش یک فراخوانی مدل
      است، پس عدد همان هزینه است و نامِ خوش‌آهنگ فقط پنهانش می‌کرد.

      ورودی نامعتبر بلند می‌شکند (`abc`، `0`، `-3`، `2.5`، `101` همه با کد ۲
      رد شدند)، چون پرچمِ بی‌صدا نادیده‌گرفته‌شده یعنی کاربر فکر می‌کند عمق را
      عوض کرده و نکرده. در رابط گرافیکی هم کنترل «عمق کاوش» اضافه شد.

      اثبات: `--depth 1` روی سناریویی با `maxSteps: 10` دقیقاً یک قدم کاوش و
      یک فراخوانی مدل داد.

### سناریو به‌شکل داده، نه کد

- [x] فرمت YAML و مستندش (`scenarios/README.md`)
- [x] مفسر قدم‌ها: `go` `click` `fill` `check` `press` `wait` `set` `download`
      `when/then` `expect` `assert` `note` `dismissBlockers` `answerDialog` `clearState`
- [x] تفکیک `expect` (سخت) از `assert` (نرم — یافته ثبت می‌کند و ادامه می‌دهد)
- [x] حل قطعی قدم به ترتیب: `testid` ← `role` ← `label` ← `text`
- [x] جای‌گذاری متغیر با فیلتر: `{{identity.email | upperFirst}}`
- [x] گروه‌بندی با `as:` — تا خط زمانی خوانا بماند
- [x] `status: draft | approved` — پیش‌نویس در عنوان علامت می‌خورد
- [x] رد صریحِ `do:` (زبان طبیعی) با پیام «به فاز ۲ نیاز دارد»
- [x] فعل‌های `dblclick` ، `reload` ، `query` ، `request`
- [x] `state.sql` در کانفیگ هدف — موتور می‌داند *که* بخواند، هدف می‌داند *چطور*
- [x] مهاجرت: `email-case` ، `email-whitespace` ، `refresh-recovery` ،
      `double-submit` ، `api-validation`
- [x] مهاجرت `signup-recovery` — با `download` و `set` و مقایسهٔ فایل با صفحه
- [x] فعل `type` — تایپ با رخداد واقعی کیبورد، روی contenteditable هم کار می‌کند
- [x] فعل `hover` — بخش‌هایی از رابط فقط با hover ظاهر می‌شوند
- [x] مهاجرت `content-roundtrip` — به دو فایل شکست: `content-roundtrip.yml`
      (رفت‌وبرگشت متن) و `duplicate-title-race.yml` (پنجرهٔ بررسی یکتایی).
      **دیگر هیچ سناریوی اسکریپتی در `scenarios/nepi/` نمانده.**

      قابلیت تازه‌ای به موتور اضافه نشد. `bookId` از آدرس درنیامد چون
      `ORDER BY rowid DESC LIMIT 1` همان را می‌دهد، و شرطِ ترکیبیِ نسخهٔ
      اسکریپتی به دو `assert` جدا شکست — چون «پنجرهٔ رد شدن هست» و «اعتبارسنجی
      هیچ‌وقت قفل نمی‌کند» دو نقص‌اند.

      هر دو با اجرای واقعی سنجیده شدند: اولی هر ۸ قدم با صفر خطا (متن
      نیم‌فاصله‌دار سالم ذخیره و پس از رفرش دیده شد و عنوان دست‌نخورده ماند)،
      دومی همان نقصِ اصلی را ثبت کرد — این بار قطعی و بدون مسابقه.

      و یک ایراد که مهاجرت بیرون کشید: شرطِ `hidden` روی «فایل جدید» در چهار
      سناریو `nth` نداشت و در چیدمان‌هایی که دو دکمه با آن نام هست بلند
      می‌شکست. کلیکِ بعدی‌شان از قبل `nth: 0` داشت؛ شرطشان نداشت.

### قلاب‌های ریست

- [x] فعل `request` — درخواست مستقیم به API هدف، هم‌بستر با نشستِ صفحه
- [x] سرور لوکال نپی روی SQLite (`.claude/launch.json` → `nepi-api-local`)
- [x] `shell` — دستور روی همین ماشین
- [x] `http` — اندپوینت تستِ خودِ اپ
- [x] `browser` — پاکسازی وضعیت مرورگر
- [x] لحظهٔ `beforeRun` (در `globalSetup`)؛ `afterRun` هنوز صدا زده نمی‌شود
- [x] **به‌کارگیری** دروازهٔ ایمنی: قلاب مخرب روی `production` اجرا نشود.
      دروازه در `src/guard.js` است و هر سه مسیرِ نویسنده از آن رد می‌شوند:
      قلاب `shell`/`http` در `hooks.js`، فعل `request`، و فعل `query`.

      `query` تا امروز دروازه نداشت و همان خطرناک‌ترین بود: تابعِ `state.sql`
      همان ماژول دیتابیسیِ خودِ اپ را می‌راند، پس یک `DELETE FROM books` در
      سناریو واقعاً پاک می‌کرد. حالا خواندن روی هر محیطی آزاد است و نوشتن فقط
      روی `local`/`staging`.

      تشخیص «خواندنی» دو شرط دارد، چون هیچ‌کدام تنها کافی نبود: با
      `select`/`with`/`explain` شروع شود **و** هیچ‌جایش فعلِ نوشتن نباشد. شرط
      دوم برای `WITH x AS (…) DELETE FROM …` است. کامنت‌ها حذف و جمله‌های
      جداشده با `;` تک‌تک بررسی می‌شوند، وگرنه `SELECT 1; DELETE FROM users` و
      `-- select\nDELETE …` از کنارش رد می‌شدند.

      `mayRunDestructiveHooks` از `target.js` برداشته شد: کدِ مرده و منبعِ
      حقیقتِ دومِ همان تصمیم بود.

### دستگاه

- [x] انتخاب دستگاه از فهرست Playwright
- [x] `--device desktop,'Pixel 7'` → چند اجرا، چند گزارش
- [x] برچسب دستگاه روی یافته، تا «فقط موبایل» از «همه‌جا» جدا شود.
      `device` روی خودِ یافته می‌نشیند و `dedupe` آن را مثل قدم جمع می‌کند
      (`devices`).

      **دستگاه عمداً وارد اثرانگشت نشد.** اگر می‌شد، یک باگ روی دو دستگاه دو
      یافته می‌شد و `diff` همان نقص را هم‌زمان «تازه» و «رفته» نشان می‌داد —
      دقیقاً همان چیزی که اثرانگشت برای جلوگیری‌اش هست.

      اجرای واقعی `--device desktop,'Pixel 7'`: هر یافته دستگاه خودش را گرفت،
      `diff` هشدار ناهم‌جنسی داد و نقص مشترک را «مانده» شناخت، و در تریاژ از ۳۳
      گروه، ۴ گروهِ چنددستگاهی از گروهِ فقط‌دسکتاپ جدا شد.

      یافته‌های پیش از این فیلد `device` ندارند؛ تریاژ آن را از `run.device`
      جبران می‌کند، وگرنه کل تاریخِ موجود یک‌شبه «دستگاه نامعلوم» می‌شد.

### پرسونا

- [x] `novice` — تایپ حرف‌به‌حرف و مکث پیش از کلیک، تا اعتبارسنجی زنده و
      debounce واقعاً اجرا شوند
- [x] `pro` — بدون تأخیر، `fill` یکجا، برای بردنِ مسابقه‌ها
- [x] `--persona` در CLI که بر سناریو می‌چربد
- [x] فعل‌های آشوب: `back` ، `forward` ، `offline` ، `paste` ، `dblclick` ، `reload`
- [x] `chaos-navigation.yml` — نخستین سناریوی آشوب (یافتهٔ ۷ از آن آمد)
- [x] هدف `nepi-preview` روی بیلد تولیدی — تنها راهِ سنجشِ آفلاین.
      `targets/nepi-preview.config.js` + `scenarios/nepi-preview/offline.yml`.

      **نتیجه: ادعای آفلاین نپی برقرار است.** پس از قطع شبکه، ناوبری داخلی کار
      کرد و پوستهٔ اپ رندر شد — هر ۷ قدم با صفر خطا. تنها یافته همان بلاکر
      شناختهٔ «بروزرسانی دیتابیس» در قدم اول بود.

      سه چیز که این هدف روشن کرد:

      - **`state.sql` روی بیلد تولیدی وجود ندارد.** داورِ وضعیتِ هدف `nepi`
        ماژول دیتابیس را با مسیر سورس import می‌کند و آن مسیر فقط در dev هست.
        این هدف عمداً `state.sql` ندارد تا ادعای کارکردنش را نکند.
      - **یک شکافِ ایمنی.** اجرای نخست بدون seedِ `nepi:sync-base-url:v1`
        انجام شد و اپ نشانِ «آفلاین» داد، یعنی نتوانست به سرورِ پیش‌فرضش
        (`nepi.ir`) برسد. «نتوانست» شانس بود نه ضمانت. دروازهٔ `guard.js` جلوی
        `request` و `query` را می‌گیرد ولی جلوی خودِ اپ را که با کلیک کاربر
        همگام‌سازی می‌کند نمی‌گیرد؛ آن seed همین شکاف را می‌بندد. تأیید شد که
        در اجرای نهایی هیچ ترافیکی به `nepi.ir` نرفت.
      - **تایپ حرف‌به‌حرف روی بیلد تولیدی با hydration تصادم می‌کند.** با
        `novice` فرمِ ورود با ایمیلِ خالی submit شد و مرورگر
        «Please fill out this field» نشان داد. این می‌تواند نقصِ خودِ اپ باشد،
        ولی سنجیدنش کارِ سناریوی آفلاین نیست؛ آنجا ثبت‌نام فقط مقدمه است. پس
        `pro` شد و در سناریو نوشته شد که چرا.

### مخزن و تریاژ

- [x] فایل‌های اجرا روی دیسک
- [ ] `index.db` — ایندکس SQLite برای جست‌وجو بین اجراها. عملاً فوریتش رفت:
      رابط فاز ۳ همین کار را با خواندن مستقیم فایل‌ها می‌کند و تا وقتی کند
      نشده، یک ایندکسِ دیگر فقط یک منبع حقیقتِ دوم است.
- [x] وضعیت تریاژ روی یافته: `open` / `acknowledged` / `resolved` / `ignored`
      (در `/triage` رابط؛ نام‌ها انگلیسی ماندند تا با API یکی باشند)
- [x] پایداری وضعیت تریاژ بین اجراها بر اساس اثرانگشت — `triage/<هدف>.json`
      کلیدخوردهٔ اثرانگشت، با write اتمیک و قفلِ هر هدف
- [x] فایل بازتولید برای هر یافته — `runs/<runId>/repro/<اثرانگشت>.yml` و
      `userbug repro`. به‌شکل YAML، نه `repro.spec.js`، چون سناریو در این
      ابزار داده است نه کد.

### دیتا

- [x] هویت تازه در هر اجرا
- [x] به‌کارگیری `persian.js` — `nasty-titles.yml` با فعل `forEach`
- [x] تولید حجم: n یادداشت، n کتاب — `scenarios/nepi/volume.yml` با
      `forEach: { times: n }`.

      **از راه رابط ساخته می‌شود، نه `INSERT` مستقیم.** با `query` می‌شد در یک
      ثانیه صد ردیف ریخت، ولی آن داده واقعی نیست: نپی برای هر پاراگراف ردیف
      FTS می‌سازد و `sort_order` را خودش تخصیص می‌دهد. دادهٔ دست‌ساز، سنجشِ
      جست‌وجو را به دروغ سبز می‌کند.

      `times` تنها چیزی بود که به موتور اضافه شد. «n سند بساز» را نمی‌شد با
      فهرستِ ادبی نوشت جز با تکرار دستیِ n سطر، و آن‌وقت عددِ حجم در دلِ داده
      گم می‌شد. سقف ۵۰۰ دارد و ورودی نامعتبر بلند می‌شکند
      (`_selftest/foreach.spec.js`، ۴ سنجه).

      حلقه عمداً قدم‌های خودش را نمی‌سازد: ساختِ داده مقدمه است، و سنجش‌ها بعد
      از آن در گروه‌های جدا می‌آیند.

      و یک درسِ محیطی: نخستین اجرا سه یافتهٔ قلابی از
      `ERR_NETWORK_IO_SUSPENDED` داد — کروم وقتی ماشین به خواب می‌رود I/O شبکه
      را معلق می‌کند. سناریوی چنددقیقه‌ای این را می‌بیند و سناریوی چندثانیه‌ای
      نه. به allowlist اضافه شد؛ تنها خطی در آن فهرست که دربارهٔ اپ هیچ
      نمی‌گوید و دربارهٔ ماشینِ مجری می‌گوید.

      **و دو یافتهٔ قلابی دیگر که پیش از گزارش گرفته شدند** — بار پنجم که این
      تله در پروژه تکرار می‌شود. سنجش اول «یادداشت شمارهٔ 12» را روی صفحه
      می‌جست و شکست؛ عکسِ همان قدم نشان داد هر ۱۲ سند سرِ جایشان هستند و اپ
      عنوان را نرمال کرده («شمارهٔ» → «شماره»، رقم لاتین → فارسی). سنجش دوم
      روی «۱۲ کتاب» هم نخورد، چون عدد و واژه در دو عنصر جدا نشسته‌اند و
      `getByText` تک‌عنصری است.

      نسخهٔ درست، متنِ کلِ صفحه را می‌خواند و با regexی می‌سنجد که هر دو دستگاه
      رقم را می‌پذیرد. قاعده‌ای که از این درآمد: **در سنجشِ حجم به رشتهٔ
      تایپ‌شده تکیه نکنید**؛ به عددی تکیه کنید که خودِ اپ اعلام می‌کند.

**معیار پایان**

- [x] یک پروژهٔ دوم — هر چیزی جز نپی — فقط با نوشتن یک فایل کانفیگ وصل شود
      و اجرا بگیرد، بدون دست زدن به کد موتور

      `targets/userbug-ui.config.js` (رابط گرافیکی خودِ ابزار، به‌عنوان یک اپِ
      بیرونی) + دو سناریوی YAML. نتیجه: ۱۲ پاس، ۲ skip، صفر شکست، ۲۴ قدم.

      ولی «بدون دست زدن به کد موتور» **برآورده نشد** و همین ارزشِ آزمون بود.
      سه فرضِ نپی‌محور بیرون زد:

      - `testDir: './scenarios'` همهٔ `.spec.js`ها را برمی‌داشت، پس
        `scenarios/nepi/content-roundtrip.spec.js` با آدرس هدفِ تازه اجرا شد و
        شکست. حالا `testIgnore` پوشهٔ هدف‌های دیگر را کنار می‌گذارد.
      - `_selftest/cache.spec.js` به مسیر `/login` و دکمهٔ «ورود / ثبت‌نام» نپی
        وابسته بود ولی جایی اعلامش نکرده بود. حالا صریح skip می‌شود.
      - پیش‌فرض `isolation` مقدار `fresh-signup` بود، پس `run.json` برای هدفی
        که هیچ جداسازی ندارد ادعای ثبت‌نام تازه می‌کرد. حالا `none` است.

      و یک ایراد در JUnit که بدون این هدف پیدا نمی‌شد: تستِ skip‌شده مثل تست
      موفق شمرده می‌شد. حالا `<skipped/>` می‌گیرد و در `skipped` می‌آید.

---

## فاز ۲ — هوش

### ارائه‌دهنده و تنظیمات

- [x] `src/models/provider.js` — لایهٔ انتزاع، فعلاً OpenRouter
- [x] `userbug.config.example.js` — پیش‌فرض کلی و کلیدها
- [x] سه نقش: `resolve` ، `author` ، `analyze`
- [x] پیش‌فرض: `claude-haiku-4.5` برای حل و نوشتن، `claude-sonnet-5` برای تحلیل
- [x] لایه‌بندی: پیش‌فرض کلی ← هدف ← تک‌درخواست (با خودآزما)
- [x] `userbug models --free` — فهرست زندهٔ مدل‌ها، تا اسلاگ از حافظه نوشته نشود

### کش — هستهٔ اقتصادی

- [x] `src/steps/cache.js` — ساختار قدمِ کش‌شده، در گیت (سرمایه است نه فایل موقت)
- [x] `domSignature` — امضای ساختاری، مستقل از کلاس‌های تیلویند و شناسه‌های `bits-cNN`
- [x] منطق باطل‌سازی: selector نبود ← AI ؛ امضا نخواند ← AI
- [x] خودآزمای امضا: پایدار بین دو رندر، و حساس به تغییر ساختاری
- [x] heal خودکار و ثبت `healCount`
- [x] بازبینی نمونه‌ای (`reverifyEvery`)
- [x] `do:` در مفسر، و اثباتِ اجرای بدون کلید با کشِ کامل
- [x] تفکیک «بازبینی» از «heal» — بازبینیِ نمونه‌ای دیگر `healCount` را باد نمی‌کند
- [x] آمار مدل و نرخ heal در `report.html`

### کاوش و ساخت سناریو

- [x] کاوشگر بی‌سناریو با منشور، سقف قدم و بودجه
- [x] `explore.avoid` در کانفیگ هدف — عامل نتواند خودش را از بازی بیرون کند
- [x] ثبت هر قدمِ کاوش همان لحظه (نه در پایان) — تا timeout تاریخچه را نبرد
- [x] `timeout` در سطح سناریو
- [x] رفعِ ابهامِ خودکارِ توصیف با `nth`
- [x] ~~snapshot فقط دیده‌شدنی‌ها را می‌بیند ولی locator همه را~~ — توصیف‌های
      تولیدشده حالا `visible: true` دارند
- [x] نام از متنِ **دیده‌شدنی** ساخته می‌شود، نه `textContent` خام — نپی برای
      واکنش‌گرایی دو نسخه از هر برچسب دارد و یکی را پنهان می‌کند
- [x] وضعیت عناصر (`selected` ، `checked` ، `value`) در snapshot — بدون آن مدل
      دوباره همان تب را می‌زد
- [x] محافظِ تکرار در کاوش — «کارِ تکراری نکن» در prompt کافی نبود
- [x] `--author` — نویسندگی سناریو حین همان کاوش اول، بدون فراخوانی اضافه
- [x] خروجی همیشه `status: draft` و در `_drafts/` که خودکار اجرا نمی‌شود
- [x] مقدمهٔ پیش‌نویس از سناریوی میزبان، تا واقعاً اجراپذیر باشد
- [x] **اثبات:** پیش‌نویسِ تولیدشده بدون دست‌کاری اجرا شد
- [ ] `scaffold --source --goal` — ساخت از روی سورس (کاوش+author عملاً جایش
      را گرفته؛ اگر لازم نشد، حذفش کنیم)

### هزینه و ایمنی

- [x] شمارش توکن و هزینهٔ هر اجرا در `run.json` و خلاصهٔ کنسول
- [x] `budgetPerRun` — توقف و علامت‌گذاری، نه ادامهٔ بی‌صدا
- [x] `redact.js` — ماسک کردن رمز و توکن پیش از ساخت prompt (با خودآزما)

**معیار پایان**

- [ ] سناریوی یادگرفته‌شده ۱۰ بار پشت‌سرهم بدون یک تماس با مدل اجرا شود
- [ ] و اگر عمداً دکمه‌ای در سورس عوض شد، اجرای یازدهم خودش heal کند

---

## فاز ۳ — رابط گرافیکی

شروع فقط وقتی مقایسهٔ چند `report.html` دستی آزاردهنده شده باشد.

- [x] اپ SvelteKit در `ui/`
- [x] کپی کامپوننت‌های `src/lib/components/ui` از نپی
- [x] فهرست پروژه‌ها؛ ویرایش کانفیگ و سناریو **روی فایل**، نه در دیتابیس
- [x] اجرای زنده با SSE — قدم، عکس، خطا همان لحظه
- [x] صفحهٔ اجرا: خط زمانی با خطای کلاینت و سرور کنار هم
- [x] تریاژ با ادغام تکراری‌ها
- [x] مقایسهٔ دو اجرا
- [x] embed کردن trace viewer پلی‌رایت — نه بازنویسی‌اش
- [x] فقط روی `127.0.0.1` ، بدون احراز هویت

**معیار پایان**

- [ ] یک هفتهٔ کامل کار با ابزار، بدون باز کردن ترمینال

---

## فاز ۴ — تعمیم

- [ ] وصل کردن بقیهٔ پروژه‌ها، یکی‌یکی. نخستین‌شان وصل شد
      (`userbug-ui`)؛ ادامه‌اش به پروژهٔ واقعی بعدی بستگی دارد.
- [x] حالت جعبه‌سیاه برای سایتِ بدون سورس — هدفی که `logs`، `apiURL`،
      `state.sql`، `source` و `isolation` ندارد اجرا می‌شود و تنها لایهٔ رصدش
      مرورگر است. نمونه و جدول «نبودِ هر کلید یعنی چه» در README.

      آنچه در این حالت **نداریم** و باید صریح بماند: خطای سرور، داورِ وضعیت
      (`query`)، و درخواست مستقیم API. پس «صفر یافته» روی جعبه‌سیاه ضعیف‌تر از
      «صفر یافته» روی نپی است و نباید یکی خوانده شود.
- [ ] کتابخانهٔ سناریوی آماده: ثبت‌نام، ورود، پرداخت، جست‌وجو
- [x] حالت CI: خروجی JUnit، بدون رابط — `src/report/junit.js`. هر اجرا کنار
      `report.html` یک `junit.xml` هم می‌گذارد و `--junit <مسیر>` برای CI
      کپی می‌کند (با `--device a,b` هر دستگاه فایل خودش را می‌گیرد).

      سه تصمیمی که ارزش نوشتن دارند:

      - **یافته = `failure`.** سناریویی که سالم تمام شود ولی خطای کنسول بدهد
        باید بیلد را قرمز کند؛ وگرنه این ابزار در CI بی‌اثر است.
      - **وضعیت واقعی تست از `traces.ndjson`.** تستی که روی `expect` بشکند و
        هیچ یافته‌ای ثبت نکند وگرنه سبز خوانده می‌شد. سناریویی که پیش از
        نخستین قدم بیفتد هم از همان‌جا پیدا می‌شود، نه از فهرست قدم‌ها.
      - **فایل خالی ممنوع.** شکست کانفیگ یا دستگاه ناشناخته پیش از
        `globalTeardown` می‌شکند؛ CLI در آن حالت خودش فایلی با `error`
        می‌نویسد. فایل بی‌`testcase` را بیشتر CIها سبز می‌خوانند.

      زمان از جمع قدم‌ها می‌آید نه `startedAt`→`finishedAt`، چون `finish()`
      با هر `userbug report` تازه می‌شود و زمانِ دیواری بعد از یک بازسازی
      ساعت‌ها می‌شد.

      وضعیت تست هم با `testId` گروه می‌شود، نه با عنوان: `retry` بالاتر نتیجهٔ
      نهاییِ همان تست است، ولی `--repeat-each` چند تستِ جدا می‌سازد و اگر یکی
      بیفتد سناریو افتاده است. با «آخرین سطر برنده» تکرارِ افتاده زیر تکرارِ
      سالم دفن می‌شد.
- [ ] **کد خروج با محتوای `junit.xml` یک حرف نمی‌زند.** تستی که روی `expect`
      سخت بیفتد و هیچ یافته‌ای نسازد در `junit.xml` درست `failure` می‌گیرد،
      ولی `classifyExit` آن را کد ۲ («خطای اجراگر») می‌خواند و رابط گرافیکی
      هم `outcome: error` نشان می‌دهد. جهتش امن است (قرمز می‌شود) ولی برچسبش
      غلط است و README کد ۲ را «نتیجه قابل اتکا نیست» معنا کرده. تفکیکش یک
      شرط است: `run.json` همان لحظه `status: 'failed'` دارد.
- [ ] **اجرای kill‌شده با `userbug report` سبز می‌شود.** `finish()` وقتی
      وضعیتی به آن داده نشود، `running` را به `finished` تبدیل می‌کند، پس
      اجرایی که وسط راه کشته شده بعداً «تمام‌شده» خوانده می‌شود. برای همین
      `running` در دروازهٔ JUnit عملاً مرده است. راهش: وضعیت قبلیِ `running`
      به `interrupted` تبدیل شود نه `finished`.
- [ ] **وضعیت تست، محصولِ جانبیِ کپیِ trace است.** `persistTraces` فقط وقتی
      سطر می‌نویسد که ضمیمهٔ trace وجود داشته باشد، پس کرشِ worker یا پرشدن
      دیسک وضعیت واقعی تست‌ها را می‌برد. الان JUnit این حالت را با «نتیجهٔ
      ناقص» علامت می‌زند، ولی درستش این است که ثبت وضعیت از کپیِ فایل جدا شود.
- [ ] `--repeat n` در `junit.xml` یک `testcase` می‌شود، پس نسبت بی‌ثباتی
      («۱ از ۱۰») در CI دیده نمی‌شود. شمار تکرارها باید در پیام `failure`
      بیاید.
- [x] ~~تست `skip`شده در `junit.xml` شمرده نمی‌شد~~ — بدتر از آن بود: مثل تست
      موفق دیده می‌شد و شمار پاس را باد می‌کرد. حالا `<skipped/>` می‌گیرد و در
      `skipped` می‌آید؛ اجرایی که **همهٔ** تست‌هایش رد شود هم «پوشش صفر» خطا
      می‌گیرد، چون `failures="0"` در آن حالت درست ولی گمراه‌کننده است.
- [ ] رابط گرافیکی به `junit.xml` لینک نمی‌دهد؛ فایل سرو می‌شود ولی فقط با
      آدرس دستی باز می‌شود.
- [ ] استخراج invariant از سورس برای شکار باگ منطقی

---

## بدهی فنی

- [x] ~~گزارش با `--reporter=line` از دست می‌رفت~~ — نهایی‌سازی به
      `globalTeardown` منتقل شد (`src/finalize.js`) و همیشه اجرا می‌شود
- [x] ~~یافته‌های خودآزما گزارش را آلوده می‌کردند~~ — پرچم `synthetic`
- [x] ~~بستن خودکار همهٔ dialogها رفتار اپ را عوض می‌کرد~~ —
      `ub.answerDialog()` و `ub.onNextDialog()` اضافه شد
- [x] ~~اثرانگشت ذخیره‌شده بود~~ — حالا هنگام خواندن دوباره حساب می‌شود، پس
      بهبودِ نرمال‌سازی اجراهای قدیمی را هم درست می‌کند
- [x] ~~جمع‌کنندهٔ لاگ سرور روی هیچ سناریوی واقعی آزموده نشده~~ — با
      `api-validation.yml` ثابت شد. آن سناریو با فعل `request` مستقیم API را
      می‌زند، و هشدار PHP همان لحظه به همان قدم چسبید و یافته شد (یافتهٔ
      «`$key»` در `Http.php`» از همین‌جا آمد). در ۸ اجرا رخداد `source: server`
      واقعی ثبت شده و در خط زمانی کنار خطای کلاینت دیده می‌شود.

      آنچه هنوز ثابت نشده: مسیر **چرخش لاگ** (فایلی که وسط اجرا کوچک می‌شود) و
      چند جمع‌کننده به‌طور هم‌زمان. کدشان هست، اجرای واقعی‌شان نه.
- [x] یافته‌های خودآزما با پرچم `synthetic: true` از گزارش واقعی جدا می‌شوند.
- [x] ~~`--reporter=line` باعث می‌شد `report.html` ساخته نشود~~ — سنجیده شد:
      با `--reporter=line` هم `report.html` و هم `junit.xml` ساخته می‌شوند و
      `run.json` بسته می‌شود (`status: finished`). این همان چیزی است که انتقال
      نهایی‌سازی به `globalTeardown` قرار بود تضمین کند.
- [ ] **با گزارشگرِ جایگزین، `traces/` و وضعیت واقعی تست‌ها نداریم.** بازماندهٔ
      همان مسئله: نگهداری trace و وضعیت `passed`/`failed` فقط در گزارشگر ما
      انجام می‌شود، پس `playwright test --reporter=line` هیچ trace نمی‌گذارد و
      `junit.xml` نمی‌تواند تستی را که *بی‌یافته* افتاده failure کند. از راه
      `userbug run` این مسئله وجود ندارد (گزارشگر همیشه از کانفیگ می‌آید)؛
      فقط فراخوانی مستقیم پلی‌رایت آسیب‌پذیر است. راهش: وضعیت تست جایی بیرون
      از گزارشگر ثبت شود، مثلاً در fixture پایانِ هر تست.
- [ ] `dismissBlockers` سقف ۸ تکرار دارد و اگر پنجره‌ای بسته نشود، بی‌صدا رد
      می‌شود. باید بعد از سقف، خودش یافته ثبت کند.
- [ ] هیچ تست واحدی برای `oracle.js` و `run-store.js` نیست — ابزارِ تست که
      خودش تست ندارد.
- [ ] `judge()` فقط `severity: 'error'` را می‌بیند؛ هشدارها هیچ‌جا جمع نمی‌شوند.
- [x] ~~توصیفِ مبهم در شرط، یافتهٔ قلابی می‌ساخت~~ — `checkCondition` هر خطای
      locator را با `.catch(() => false)` قورت می‌داد. یعنی «توصیفِ من به سه
      عنصر خورد» و «شرط نخورد» یک جواب می‌گرفتند، و در `assert` هر دو به‌نام اپ
      یافته ثبت می‌شد. یک اجرا سه یافتهٔ قلابی داد
      («/list چیزی رندر نکرد» — عکسِ همان قدم صفحهٔ سالم را نشان می‌داد).
      حالا فقط `TimeoutError` یعنی «نخورد»؛ تطبیق چندگانه و selector نامعتبر
      بلند می‌شکنند با پیامِ «این ایرادِ سناریو است، نه اپ»
      (`src/scenario/run.js` → `conditionFailed`).
- [x] مسیرِ پایانِ هر قدم در `events.ndjson` و گزارش HTML ثبت می‌شود. پیش‌تر
      «کاربر کجا بود» فقط از عکس درمی‌آمد و برای یافته‌های ناوبری‌محور یعنی باز
      کردن سه عکس برای فهمیدن یک ردیف.
- [x] ~~کاوش، هزینهٔ خودش را گزارش نمی‌کرد~~ — `aiStats` فقط در مسیر `do:`
      شمرده می‌شد، پس اجرای کاوشِ ۳۷ قدمی در `run.json` می‌گفت `ai: null` و
      خلاصهٔ کنسول خط «مدل» را چاپ نمی‌کرد: گران‌ترین اجرا، بی‌هزینه‌ترین به
      نظر می‌رسید. بودجه از اول درست حساب می‌شد؛ فقط گزارشش گم بود. حالا
      `مدل: کش 0 · مدل 12 · 12 فراخوانی`.
- [ ] **سرنخ: تایپ حرف‌به‌حرف روی بیلد تولیدی مقدار را می‌بازد.** در نخستین
      اجرای `nepi-preview` با پرسونای `novice`، فرمِ ورود با ایمیلِ خالی
      submit شد و مرورگر «Please fill out this field» داد؛ با `pro` (که
      مقدار را یکجا می‌گذارد) مشکل رفت. اگر تایپِ کاربر واقعی در بازهٔ
      hydration بی‌صدا پاک می‌شود، این نقصِ اپ است نه ابزار — ولی تا سناریوی
      قطعیِ جداگانه‌ای نداشته باشیم، سرنخ است نه یافته.
- [ ] **کاوشگر در حلقهٔ نیتِ غلط گیر می‌کند.** در `کاوش editor` ده قدم از
      دوازده قدم صرف hover روی «فایل جدید» و «پوشه جدید» شد، به امیدِ منویی با
      گزینهٔ «سند» که وجود ندارد. سقفِ «هر عنصر سه بار» جلوی تکرارِ **عنصر** را
      می‌گیرد، نه تکرارِ **فرض**. سناریو صفر خطا داد ولی به ویرایشگر هم نرسید،
      و «صفر یافته» در این حالت یعنی «نرسیدم»، نه «سالم بود».
      یک راهش: نیتِ تکرارشده (`why` مشابه) هم مثل عنصرِ تکرارشده سقف داشته
      باشد و در تاریخچه صریح بنویسد «این فرض غلط بود».
- [ ] **فعلِ «صفحهٔ ثالث»** — صفحه‌ای از مبدأ دیگر که اپ را iframe یا
      `window.open` کند و بتواند به آن `postMessage` بزند. یافتهٔ ۹ نپی
      (شنوندهٔ `NEPI_AUTH_TOKEN` بدون بررسی `event.origin`) بدون این قابلیت
      قابل بازتولید نیست و از سورس تأیید شده مانده. `page.route()` می‌تواند
      صفحه‌ای روی مبدأ ساختگی سرو کند، پس راهش بسته نیست.
