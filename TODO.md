# userbug — فهرست ساخت

فهرست کارِ **خودِ برنامه**. یافته‌های اپ‌های تحت تست اینجا نیست — آن‌ها در
`findings/` می‌نشینند.

طرح کامل: [Artifact](https://claude.ai/code/artifact/8c2e8721-f2ec-4724-8aa2-65ffbf28582d)

**وضعیت:** فاز ۰ و ۱ بسته شدند. هستهٔ فاز ۲ کار می‌کند و معیار پایانش برآورده
شده: مدل مسیر را یاد می‌گیرد، ده اجرای بعدی بدون هیچ تماسی می‌روند، و تغییرِ
عمدی فقط همان یک قدم را heal می‌کند. مدل‌های پیش‌فرض رایگان‌اند.
کاوش آزاد، نویسندگیِ پیش‌نویس، بازتولیدِ خودکار و گزارش آمار مدل هم کار
می‌کنند. **فاز ۲ تمام است.** فاز ۳ (رابط) طبق معیار خودمان هنوز وقتش نرسیده.

---

## فاز ۰ — برش عمودی

اثبات اینکه زنجیره کار می‌کند. عمداً کوچک، عمداً بدون CLI و بدون AI.

- [x] ساخت پروژه، نصب `@playwright/test` و `@faker-js/faker` و chromium
- [x] `targets/nepi.config.js` — فقط آدرس و دسترسی، بدون دستور اجرا
- [x] `src/target.js` — بارگذاری هدف + دروازهٔ ایمنی `environment`
- [x] `playwright.config.js` — انتخاب دستگاه از `UB_DEVICE`
- [x] `src/global-setup.js` — هر اجرا پوشهٔ خودش
- [x] `src/observe/client.js` — console، pageerror، شبکه، ۴xx/۵xx، dialog
- [x] اسکریپت اولیه برای `unhandledrejection` (Playwright خودش نمی‌دهد)
- [x] `src/observe/server.js` — tail فایل با آفست، مقاوم به چرخش لاگ
- [x] `src/observe/oracle.js` — داور، allowlist، اثرانگشت، ادغام
- [x] `src/store/run-store.js` — `run.json` ، `events.ndjson` ، `findings.ndjson` ، `shots/`
- [x] `src/report/html.js` + `src/reporter.js` — گزارش تک‌فایلی
- [x] `src/fixtures.js` — fixture خودکار `auto: true` روی همهٔ تست‌ها
- [x] `ub.step()` — مرز زمانی هر قدم، عکس، و لاگ سرورِ همان بازه
- [x] `ub.note()` — یافتهٔ تشخیصِ سناریو
- [x] `ub.dismissBlockers()` — بستن پنجرهٔ مزاحم مثل کاربر واقعی، با ثبت
- [x] `ub.clearBrowserState()` — کوکی، localStorage، IndexedDB، cache، SW، **OPFS**
- [x] `src/data/persian.js` — هویت تازه + رشته‌های بدخیم فارسی
- [x] خودآزمای رصدگر — تزریق عمدی خطا، تا «صفر خطا» با «رصدگر خاموش» اشتباه نشود
- [x] اجرای دو دستگاهی: `desktop` و `Pixel 7`

**معیار پایان**

- [x] پنج باگ ناشناخته در نپی — کامل شد (نگاه: `findings/nepi.md`)

---

## فاز ۱ — موتور و قرارداد

هنوز بدون AI. اینجا چیزی که در فاز ۰ اسکریپت بود، برنامه می‌شود.

### CLI — `bin/userbug.js`

- [x] `run <target> [--scenario] [--grep] [--device] [--headed] [--repeat]`
- [x] `report <runId|latest>` — بازسازی گزارش از مخزن، بدون اجرای دوباره
- [x] `list` — فهرست اجراها با وضعیت و تعداد یافته
- [x] `diff <runA> <runB>` — چه یافته‌ای تازه است و چه یافته‌ای رفته
- [x] شناسهٔ اجرا با «latest» یا پیشوندِ یکتا
- [x] `replay <runId> [--only-findings]` — همان هدف، همان دستگاه، همان سناریوها
- [x] ثبت فهرست سناریوها در `run.json` تا `replay` بداند چه را دوباره ببرد
- [ ] ~~`resume <runId>`~~ — **کنار گذاشته شد.** ادامه از قدمی که ماند یعنی
      بازگرداندن نشست و دیتابیس محلی و کش به همان نقطه؛ مرورگر راهی برایش
      نمی‌دهد. به‌جایش `replay --only-findings` هست. اگر روزی لازم شد، راهش
      عکس‌برداری از وضعیت است نه ادامه دادن.
- [ ] `--persona` و `--depth` (هنوز پرسونایی نیست)

### سناریو به‌شکل داده، نه کد

- [x] فرمت YAML و مستندش (`scenarios/README.md`)
- [x] مفسر قدم‌ها: `go` `click` `fill` `check` `press` `wait` `set` `download`
      `when/then` `expect` `assert` `note` `dismissBlockers` `answerDialog` `clearState`
- [x] تفکیک `expect` (سخت) از `assert` (نرم — یافته ثبت می‌کند و ادامه می‌دهد)
- [x] حل قطعی قدم به ترتیب: `testid` ← `role` ← `label` ← `text`
- [x] جای‌گذاری متغیر با فیلتر: `{{identity.email | upperFirst}}`
- [x] گروه‌بندی با `as:` — تا خط زمانی خوانا بماند
- [x] `status: draft | approved` — پیش‌نویس در عنوان علامت می‌خورد
- [x] رد صریحِ `do:` (زبان طبیعی) با پیام «به فاز ۲ نیاز دارد»
- [x] فعل‌های `dblclick` ، `reload` ، `query` ، `request`
- [x] `state.sql` در کانفیگ هدف — موتور می‌داند *که* بخواند، هدف می‌داند *چطور*
- [x] مهاجرت: `email-case` ، `email-whitespace` ، `refresh-recovery` ،
      `double-submit` ، `api-validation`
- [x] مهاجرت `signup-recovery` — با `download` و `set` و مقایسهٔ فایل با صفحه
- [x] فعل `type` — تایپ با رخداد واقعی کیبورد، روی contenteditable هم کار می‌کند
- [x] فعل `hover` — بخش‌هایی از رابط فقط با hover ظاهر می‌شوند
- [ ] مهاجرت `content-roundtrip` (حالا فعل‌هایش هست)

### قلاب‌های ریست

- [x] فعل `request` — درخواست مستقیم به API هدف، هم‌بستر با نشستِ صفحه
- [x] سرور لوکال نپی روی SQLite (`.claude/launch.json` → `nepi-api-local`)
- [x] `shell` — دستور روی همین ماشین
- [x] `http` — اندپوینت تستِ خودِ اپ
- [x] `browser` — پاکسازی وضعیت مرورگر
- [x] لحظهٔ `beforeRun` (در `globalSetup`)؛ `afterRun` هنوز صدا زده نمی‌شود
- [ ] **به‌کارگیری** دروازهٔ ایمنی: قلاب مخرب روی `production` اجرا نشود
  (تابعش در `target.js` هست ولی هنوز هیچ‌جا صدا زده نمی‌شود)

### دستگاه

- [x] انتخاب دستگاه از فهرست Playwright
- [x] `--device desktop,'Pixel 7'` → چند اجرا، چند گزارش
- [ ] برچسب دستگاه روی یافته، تا «فقط موبایل» از «همه‌جا» جدا شود

### پرسونا

- [x] `novice` — تایپ حرف‌به‌حرف و مکث پیش از کلیک، تا اعتبارسنجی زنده و
      debounce واقعاً اجرا شوند
- [x] `pro` — بدون تأخیر، `fill` یکجا، برای بردنِ مسابقه‌ها
- [x] `--persona` در CLI که بر سناریو می‌چربد
- [x] فعل‌های آشوب: `back` ، `forward` ، `offline` ، `paste` ، `dblclick` ، `reload`
- [x] `chaos-navigation.yml` — نخستین سناریوی آشوب (یافتهٔ ۷ از آن آمد)
- [ ] هدف `nepi-preview` روی بیلد تولیدی — تنها راهِ سنجشِ آفلاین

### مخزن و تریاژ

- [x] فایل‌های اجرا روی دیسک
- [ ] `index.db` — ایندکس SQLite برای جست‌وجو بین اجراها
- [ ] وضعیت تریاژ روی یافته: `تازه` / `تأییدشده` / `رفع‌شده` / `بی‌اهمیت`
- [ ] پایداری وضعیت تریاژ بین اجراها بر اساس اثرانگشت
- [ ] `repro.spec.js` — اسکریپت بازتولید برای هر یافته

### دیتا

- [x] هویت تازه در هر اجرا
- [x] به‌کارگیری `persian.js` — `nasty-titles.yml` با فعل `forEach`
- [ ] تولید حجم: n یادداشت، n کتاب — برای دیدن رفتار در داده زیاد

**معیار پایان**

- [ ] یک پروژهٔ دوم — هر چیزی جز نپی — فقط با نوشتن یک فایل کانفیگ وصل شود
      و اجرا بگیرد، بدون دست زدن به کد موتور

---

## فاز ۲ — هوش

### ارائه‌دهنده و تنظیمات

- [x] `src/models/provider.js` — لایهٔ انتزاع، فعلاً OpenRouter
- [x] `userbug.config.example.js` — پیش‌فرض کلی و کلیدها
- [x] سه نقش: `resolve` ، `author` ، `analyze`
- [x] پیش‌فرض: `claude-haiku-4.5` برای حل و نوشتن، `claude-sonnet-5` برای تحلیل
- [x] لایه‌بندی: پیش‌فرض کلی ← هدف ← تک‌درخواست (با خودآزما)
- [x] `userbug models --free` — فهرست زندهٔ مدل‌ها، تا اسلاگ از حافظه نوشته نشود

### کش — هستهٔ اقتصادی

- [x] `src/steps/cache.js` — ساختار قدمِ کش‌شده، در گیت (سرمایه است نه فایل موقت)
- [x] `domSignature` — امضای ساختاری، مستقل از کلاس‌های تیلویند و شناسه‌های `bits-cNN`
- [x] منطق باطل‌سازی: selector نبود ← AI ؛ امضا نخواند ← AI
- [x] خودآزمای امضا: پایدار بین دو رندر، و حساس به تغییر ساختاری
- [x] heal خودکار و ثبت `healCount`
- [x] بازبینی نمونه‌ای (`reverifyEvery`)
- [x] `do:` در مفسر، و اثباتِ اجرای بدون کلید با کشِ کامل
- [x] تفکیک «بازبینی» از «heal» — بازبینیِ نمونه‌ای دیگر `healCount` را باد نمی‌کند
- [x] آمار مدل و نرخ heal در `report.html`

### کاوش و ساخت سناریو

- [x] کاوشگر بی‌سناریو با منشور، سقف قدم و بودجه
- [x] `explore.avoid` در کانفیگ هدف — عامل نتواند خودش را از بازی بیرون کند
- [x] ثبت هر قدمِ کاوش همان لحظه (نه در پایان) — تا timeout تاریخچه را نبرد
- [x] `timeout` در سطح سناریو
- [x] رفعِ ابهامِ خودکارِ توصیف با `nth`
- [x] ~~snapshot فقط دیده‌شدنی‌ها را می‌بیند ولی locator همه را~~ — توصیف‌های
      تولیدشده حالا `visible: true` دارند
- [x] نام از متنِ **دیده‌شدنی** ساخته می‌شود، نه `textContent` خام — نپی برای
      واکنش‌گرایی دو نسخه از هر برچسب دارد و یکی را پنهان می‌کند
- [x] وضعیت عناصر (`selected` ، `checked` ، `value`) در snapshot — بدون آن مدل
      دوباره همان تب را می‌زد
- [x] محافظِ تکرار در کاوش — «کارِ تکراری نکن» در prompt کافی نبود
- [x] `--author` — نویسندگی سناریو حین همان کاوش اول، بدون فراخوانی اضافه
- [x] خروجی همیشه `status: draft` و در `_drafts/` که خودکار اجرا نمی‌شود
- [x] مقدمهٔ پیش‌نویس از سناریوی میزبان، تا واقعاً اجراپذیر باشد
- [x] **اثبات:** پیش‌نویسِ تولیدشده بدون دست‌کاری اجرا شد
- [ ] `scaffold --source --goal` — ساخت از روی سورس (کاوش+author عملاً جایش
      را گرفته؛ اگر لازم نشد، حذفش کنیم)

### هزینه و ایمنی

- [x] شمارش توکن و هزینهٔ هر اجرا در `run.json` و خلاصهٔ کنسول
- [x] `budgetPerRun` — توقف و علامت‌گذاری، نه ادامهٔ بی‌صدا
- [x] `redact.js` — ماسک کردن رمز و توکن پیش از ساخت prompt (با خودآزما)

**معیار پایان**

- [ ] سناریوی یادگرفته‌شده ۱۰ بار پشت‌سرهم بدون یک تماس با مدل اجرا شود
- [ ] و اگر عمداً دکمه‌ای در سورس عوض شد، اجرای یازدهم خودش heal کند

---

## فاز ۳ — رابط گرافیکی

شروع فقط وقتی مقایسهٔ چند `report.html` دستی آزاردهنده شده باشد.

- [ ] اپ SvelteKit در `ui/`
- [ ] کپی کامپوننت‌های `src/lib/components/ui` از نپی
- [ ] فهرست پروژه‌ها؛ ویرایش کانفیگ و سناریو **روی فایل**، نه در دیتابیس
- [ ] اجرای زنده با SSE — قدم، عکس، خطا همان لحظه
- [ ] صفحهٔ اجرا: خط زمانی با خطای کلاینت و سرور کنار هم
- [ ] تریاژ با ادغام تکراری‌ها
- [ ] مقایسهٔ دو اجرا
- [ ] embed کردن trace viewer پلی‌رایت — نه بازنویسی‌اش
- [ ] فقط روی `127.0.0.1` ، بدون احراز هویت

**معیار پایان**

- [ ] یک هفتهٔ کامل کار با ابزار، بدون باز کردن ترمینال

---

## فاز ۴ — تعمیم

- [ ] وصل کردن بقیهٔ پروژه‌ها، یکی‌یکی
- [ ] حالت جعبه‌سیاه برای سایتِ بدون سورس
- [ ] کتابخانهٔ سناریوی آماده: ثبت‌نام، ورود، پرداخت، جست‌وجو
- [ ] حالت CI: خروجی JUnit، بدون رابط
- [ ] استخراج invariant از سورس برای شکار باگ منطقی

---

## بدهی فنی

- [x] ~~گزارش با `--reporter=line` از دست می‌رفت~~ — نهایی‌سازی به
      `globalTeardown` منتقل شد (`src/finalize.js`) و همیشه اجرا می‌شود
- [x] ~~یافته‌های خودآزما گزارش را آلوده می‌کردند~~ — پرچم `synthetic`
- [x] ~~بستن خودکار همهٔ dialogها رفتار اپ را عوض می‌کرد~~ —
      `ub.answerDialog()` و `ub.onNextDialog()` اضافه شد
- [x] ~~اثرانگشت ذخیره‌شده بود~~ — حالا هنگام خواندن دوباره حساب می‌شود، پس
      بهبودِ نرمال‌سازی اجراهای قدیمی را هم درست می‌کند
- [ ] **جمع‌کنندهٔ لاگ سرور روی هیچ سناریوی واقعی آزموده نشده.** جریان ثبت‌نام
      نپی کاملاً سمت کلاینت است. تا یک سناریوی همگام‌سازی نداشته باشیم، این
      تکه ثابت‌نشده می‌ماند.
- [x] یافته‌های خودآزما با پرچم `synthetic: true` از گزارش واقعی جدا می‌شوند.
- [ ] `--reporter=line` در خط فرمان، گزارشگر ما را دور می‌زند و `report.html`
      ساخته نمی‌شود. باید گزارشگر از راه دیگری اجباری شود.
- [ ] `dismissBlockers` سقف ۸ تکرار دارد و اگر پنجره‌ای بسته نشود، بی‌صدا رد
      می‌شود. باید بعد از سقف، خودش یافته ثبت کند.
- [ ] هیچ تست واحدی برای `oracle.js` و `run-store.js` نیست — ابزارِ تست که
      خودش تست ندارد.
- [ ] `judge()` فقط `severity: 'error'` را می‌بیند؛ هشدارها هیچ‌جا جمع نمی‌شوند.
- [x] ~~توصیفِ مبهم در شرط، یافتهٔ قلابی می‌ساخت~~ — `checkCondition` هر خطای
      locator را با `.catch(() => false)` قورت می‌داد. یعنی «توصیفِ من به سه
      عنصر خورد» و «شرط نخورد» یک جواب می‌گرفتند، و در `assert` هر دو به‌نام اپ
      یافته ثبت می‌شد. یک اجرا سه یافتهٔ قلابی داد
      («/list چیزی رندر نکرد» — عکسِ همان قدم صفحهٔ سالم را نشان می‌داد).
      حالا فقط `TimeoutError` یعنی «نخورد»؛ تطبیق چندگانه و selector نامعتبر
      بلند می‌شکنند با پیامِ «این ایرادِ سناریو است، نه اپ»
      (`src/scenario/run.js` → `conditionFailed`).
- [x] مسیرِ پایانِ هر قدم در `events.ndjson` و گزارش HTML ثبت می‌شود. پیش‌تر
      «کاربر کجا بود» فقط از عکس درمی‌آمد و برای یافته‌های ناوبری‌محور یعنی باز
      کردن سه عکس برای فهمیدن یک ردیف.
- [x] ~~کاوش، هزینهٔ خودش را گزارش نمی‌کرد~~ — `aiStats` فقط در مسیر `do:`
      شمرده می‌شد، پس اجرای کاوشِ ۳۷ قدمی در `run.json` می‌گفت `ai: null` و
      خلاصهٔ کنسول خط «مدل» را چاپ نمی‌کرد: گران‌ترین اجرا، بی‌هزینه‌ترین به
      نظر می‌رسید. بودجه از اول درست حساب می‌شد؛ فقط گزارشش گم بود. حالا
      `مدل: کش 0 · مدل 12 · 12 فراخوانی`.
- [ ] **کاوشگر در حلقهٔ نیتِ غلط گیر می‌کند.** در `کاوش editor` ده قدم از
      دوازده قدم صرف hover روی «فایل جدید» و «پوشه جدید» شد، به امیدِ منویی با
      گزینهٔ «سند» که وجود ندارد. سقفِ «هر عنصر سه بار» جلوی تکرارِ **عنصر** را
      می‌گیرد، نه تکرارِ **فرض**. سناریو صفر خطا داد ولی به ویرایشگر هم نرسید،
      و «صفر یافته» در این حالت یعنی «نرسیدم»، نه «سالم بود».
      یک راهش: نیتِ تکرارشده (`why` مشابه) هم مثل عنصرِ تکرارشده سقف داشته
      باشد و در تاریخچه صریح بنویسد «این فرض غلط بود».
- [ ] **فعلِ «صفحهٔ ثالث»** — صفحه‌ای از مبدأ دیگر که اپ را iframe یا
      `window.open` کند و بتواند به آن `postMessage` بزند. یافتهٔ ۹ نپی
      (شنوندهٔ `NEPI_AUTH_TOKEN` بدون بررسی `event.origin`) بدون این قابلیت
      قابل بازتولید نیست و از سورس تأیید شده مانده. `page.route()` می‌تواند
      صفحه‌ای روی مبدأ ساختگی سرو کند، پس راهش بسته نیست.
