{
  "name": "userbug",
  "version": "0.0.1",
  "description": "شبیه‌ساز کاربر برای تست خودکار اپ‌های وب",
  "main": "index.js",
  "scripts": {
    "postinstall": "node scripts/install-ui.cjs",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "ub": "node bin/userbug.js",
    "ui:deps": "node scripts/install-ui.cjs",
    "ui:dev": "npm --prefix ui run dev",
    "ui:check": "npm --prefix ui run check",
    "ui:build": "npm --prefix ui run build",
    "ui:start": "npm --prefix ui run start",
    "preui": "npm --prefix ui run build",
    "ui": "npm --prefix ui run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@faker-js/faker": "^10.6.0",
    "@playwright/test": "^1.62.1"
  },
  "bin": {
    "userbug": "./bin/userbug.js"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
