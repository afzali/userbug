# ساخته‌شده از متنِ کاربر با هوش مصنوعی.
#
# متنِ اصلی:
#   ورود و ثبت نام
#
# چیزهایی که مدل حدس زده و باید بازبینی شود:
#   کاربر فقط عنوان «ورود و ثبت نام» و آدرس پایه را داده است؛ مشخص نکرده که فرم ورود باید پر شود، ثبت‌نام جدید ساخته شود یا فقط صفحه بررسی شود. بنابراین فقط باز شدن صفحه و دیدن عنوان ورود سنجیده می‌شود تا ادامه‌اش با توضیحات بعدی کاربر مشخص شود.
#
# تا وقتی `status: draft` است، رگرسیون شمرده نمی‌شود. پس از بازبینی
# `approved` کنید.
name: ورود و ثبت‌نام در نپی
status: approved
persona: novice
steps:
  - go: /
  - expect:
      visible:
        role: heading
        name: ورود
