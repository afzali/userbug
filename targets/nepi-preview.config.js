/**
 * هدف: نپی روی بیلد تولیدی — تنها راهِ سنجشِ ادعای آفلاین.
 *
 * ── چرا سرور توسعه کافی نبود ──
 *
 * ویت در حالت dev ماژول‌ها را در لحظه از شبکه می‌دهد، پس اپِ dev ذاتاً آفلاین
 * کار نمی‌کند و «آفلاین نشد» روی آن هیچ چیزی دربارهٔ اپ نمی‌گوید. مهم‌تر:
 * `+layout.svelte` سرویس‌ورکر را فقط وقتی ثبت می‌کند که `!dev` باشد — و در
 * حالت توسعه حتی سرویس‌ورکرهای جامانده را هم عمداً برمی‌دارد.
 *
 * یعنی روی هدف `nepi`، سنجشِ آفلاین نه فقط بی‌معنا بود، بلکه **همیشه** یافتهٔ
 * قلابی می‌داد. این هدف برای همین جدا شد.
 *
 * ── چرا `state.sql` ندارد ──
 *
 * داورِ وضعیتِ هدف `nepi` با `import('/src/lib/db/database.js')` کار می‌کند و
 * آن مسیر فقط وقتی هست که ویت سورس را به‌شکل ESM سرو کند. روی بیلد تولیدی
 * چنین مسیری وجود ندارد، پس فعل `query` روی این هدف کار نمی‌کند — و نباید
 * ادعای کارکردنش را بکنیم. اگر روزی لازم شد، جایش یک قلاب اعلام‌شدهٔ خودِ
 * پروژه است، نه حدسی از سمت ابزار.
 *
 * ── چرا قلاب ریست ندارد ──
 *
 * این هدف به سرور API لوکال وصل نیست و `apiURL` هم ندارد. هر اجرا با
 * `clearState` دیتابیسِ مرورگر را از صفر می‌سازد و ثبت‌نام نپی سمت کلاینت است،
 * پس ریستِ سروری لازم نیست.
 *
 * ── راه‌اندازی ──
 *
 * در پوشهٔ نپی:
 *
 *   npm run build:noversion   # نسخه و changelog را دست نمی‌زند
 *   npm run preview           # روی ۴۱۷۳
 *
 * `build:noversion` عمدی است: `build` معمولی نسخه را از تعداد کامیت‌ها جلو
 * می‌برد و فایل changelog می‌سازد. یک بیلدِ آزمایشی نباید تاریخِ نسخه‌های
 * پروژه را دست بزند.
 */
export default {
  name: 'nepi-preview',
  baseURL: 'http://localhost:4173',

  /**
   * بیلد تولیدی است، ولی داده‌اش محلی و در همین مرورگر.
   *
   * `local` اینجا درست است نه خوش‌بینانه: هیچ کاربر واقعی و هیچ دادهٔ واقعی
   * در میان نیست. اگر روزی این هدف به سروری وصل شد که داده‌اش مال کسی است،
   * همین کلید باید `production` شود تا دروازهٔ ایمنی جلوی نوشتن را بگیرد.
   */
  environment: 'local',

  // همان سرور لوکالِ هدف `nepi`. اینجا فقط برای این است که اگر روزی سناریویی
  // فعل `request` بخواهد، آدرسش لوکال باشد نه nepi.ir.
  apiURL: 'http://127.0.0.1:8081',

  device: 'desktop',
  locale: 'fa',
  dir: 'rtl',

  logs: [],

  allowlist: [
    /favicon/i,
    // در بیلد تولیدی، ثبتِ سرویس‌ورکر و بررسی نسخه می‌توانند پیش از کامل شدن
    // پیش‌کش یک بار شکست بخورند. این نویز است نه نقص؛ اگر روزی از این فهرست
    // بلندتر شد، یعنی داریم ایراد واقعی را زیر فرش می‌کنیم.
    /service-worker/i,
  ],

  isolation: {
    mode: 'browser',
    reset: {
      beforeScenario: [
        { type: 'browser', clear: ['cookies', 'localStorage', 'indexedDB', 'cache', 'serviceWorker'] },
      ],
    },

    seed: {
      localStorage: {
        // همان مزاحمی که یافته‌اش در `findings/nepi.md` ثبت شده. توضیح کامل
        // در `targets/nepi.config.js`.
        nepi_storage_permission_dismissed: 'true',

        /**
         * اپ را صریح به سرور لوکال وصل کن — این یک الزامِ ایمنی است.
         *
         * بیلد تولیدی پیش‌فرضِ خودش را دارد و آن پیش‌فرض `nepi.ir` است. اجرای
         * نخست همین هدف بدون این کلید انجام شد و اپ نشانِ «آفلاین» نشان داد،
         * یعنی نتوانست به سرورِ پیش‌فرض برسد. «نتوانست» شانس بود، نه ضمانت:
         * روی ماشینی با دسترسی باز، همان اجرا به دادهٔ واقعی می‌خورد.
         *
         * قانون ۸ می‌گوید کارِ نویسنده روی محیط تولیدی ممنوع است. دروازهٔ
         * `guard.js` جلوی `request` و `query` را می‌گیرد، ولی جلوی خودِ اپ را
         * که با کلیک کاربر همگام‌سازی می‌کند نمی‌گیرد. این کلید همان شکاف را
         * می‌بندد.
         */
        'nepi:sync-base-url:v1': 'http://127.0.0.1:8081',
      },
    },
  },

  explore: {
    avoid: ['خروج', 'ریست کامل', 'حذف حساب', 'پاک کردن', 'Toggle Sidebar'],
  },
};
