/**
 * هدف: رابط گرافیکی خودِ userbug — نمونهٔ «جعبه‌سیاه».
 *
 * ── چرا این فایل وجود دارد ──
 *
 * ادعای فاز ۱ این بود که «یک پروژهٔ دوم فقط با نوشتن یک فایل کانفیگ وصل شود،
 * بدون دست زدن به کد موتور». تا وقتی امتحان نشده باشد، فقط یک ادعاست — و
 * موتوری که فقط روی نپی اجرا شده، لاجرم فرض‌های نپی را در خودش دارد.
 *
 * پس این هدف عمداً همه‌چیزِ اختیاری را **ندارد**:
 *
 *   بدون `logs`      → هیچ لاگ سروری خوانده نمی‌شود
 *   بدون `apiURL`    → فعل `request` روی این هدف معنا ندارد
 *   بدون `state.sql` → فعل `query` هم
 *   بدون `source`    → مدل به سورس دسترسی ندارد
 *   بدون `isolation` → نه ثبت‌نام، نه ریست دیتابیس، نه seed
 *
 * یعنی همان وضعیتی که یک سایت بیرونی بدون سورس دارد: فقط آدرس و مرورگر.
 * چیزی که باقی می‌ماند رصدگر کلاینت است — خطای جاوااسکریپت، promise رهاشده،
 * پاسخ ۴xx/۵xx، و پنجرهٔ مزاحم. برای یک هدفِ ناشناخته همین همه‌چیزِ ماست.
 *
 * ── نکتهٔ ایمنی ──
 *
 * این رابط می‌تواند اجرا شروع کند و روی فایل سناریو بنویسد. سناریوهای این
 * هدف عمداً فقط خواندنی‌اند، و `explore.avoid` جلوی کاوشِ آزاد را می‌گیرد تا
 * عاملی که آزاد می‌گردد، اجرای تازه راه نیندازد یا فایلی را بازنویسی نکند.
 *
 * ── راه‌اندازی ──
 *
 * مثل نپی، بالاآوردن سرور کارِ خودِ پروژه است:
 *
 *   npm run ui:build && npm run ui:start
 */
export default {
  name: 'userbug-ui',
  baseURL: 'http://127.0.0.1:4174',

  // رابط فقط روی loopback گوش می‌دهد و داده‌اش پوشهٔ `runs/` همین مخزن است.
  environment: 'local',

  device: 'desktop',
  locale: 'fa',
  dir: 'rtl',

  /**
   * خطاهایی که نباید قدم را قرمز کنند.
   *
   * کوتاه است و باید کوتاه بماند. اگر روزی بلند شد، یعنی داریم ایرادِ خودِ
   * رابط را زیر فرش می‌کنیم — که از همه‌جا بدتر است، چون این همان ابزاری است
   * که قرار بود ایراد پیدا کند.
   */
  allowlist: [/favicon/i],

  /**
   * عناصری که کاوشِ آزاد نباید لمس کند.
   *
   * «اجرا» و «لغو» فرآیند تازه می‌سازند، و «ذخیره»/«ساخت فایل» روی سناریوهای
   * واقعی مخزن می‌نویسند. عاملی که آزاد بگردد دیر یا زود هر دو را می‌زند.
   */
  explore: {
    avoid: ['اجرا', 'لغو', 'ذخیره', 'ساخت فایل', 'اعتبارسنجی و ذخیره', 'تغییر پوسته'],
  },
};
