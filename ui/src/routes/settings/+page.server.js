import { effectiveModels } from '../../../../src/models/settings.js';

/**
 * تنظیماتِ هوش مصنوعی — سراسری، نه در فضای کاریِ یک پروژه.
 *
 * کلید و مدل به همهٔ پروژه‌ها می‌خورند. گذاشتنشان زیرِ `/projects/<کلید>/`
 * یعنی کاربر فکر کند مالِ همان پروژه‌اند — و بعد در پروژهٔ دوم دوباره
 * دنبالشان بگردد.
 *
 * سنجشِ زندهٔ مدل‌ها اینجا انجام **نمی‌شود**: هر بار باز کردنِ صفحه یک
 * درخواست به ارائه‌دهنده می‌شد. با دکمه، وقتی کاربر خواست.
 */
export async function load() {
  return { settings: await effectiveModels() };
}
